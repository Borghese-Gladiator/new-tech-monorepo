---
description: List all Agent Workbench runs with their status, repo, branch, and last-update time. Use when the user asks what runs exist.
---

# /runs

Thin wrapper around `agent-workbench list`.

```bash
agent-workbench list
agent-workbench list --status building
```
