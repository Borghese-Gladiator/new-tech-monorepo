# Build — Goodbye command, pass 1

## Implementation summary

Wired the dispatch case but forgot to actually print the message.

## Files changed

- `bin/cli`

## Acceptance criteria coverage

| AC    | Status      | Notes                                       |
|-------|-------------|---------------------------------------------|
| AC-1  | not covered | Subcommand exits 0 but stdout is empty.     |
