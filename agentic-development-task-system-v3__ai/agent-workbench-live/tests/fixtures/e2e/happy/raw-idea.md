Add a `hello` command to the CLI that prints a greeting message.

The throwaway repo already has a `bin/cli` shell script; the goal is to add a
single new subcommand that prints `hello, world` and exits 0.
