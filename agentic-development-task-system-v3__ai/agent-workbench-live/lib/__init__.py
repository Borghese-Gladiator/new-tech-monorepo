"""Agent Workbench library modules.

Public surface used by bin/agent-workbench:

    from lib import config, metadata, events, transitions, locks, repos, audit, run_ids

Every module is stdlib-only. No external dependencies.
"""
