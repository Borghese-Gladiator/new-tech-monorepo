PYTHONPATH=. python -m unittest tests.test_run_ids -v
PYTHONPATH=. python -m unittest discover -s tests -v
python -c "from lib.cli.cmd_new_run import _canonical_repo_basename; from lib.repos import show_toplevel; # adversarial smoke script — non-existent path, two subpaths of same repo, --repo-name override semantics check, symlink-into-repo, non-git existing path, non-existent with repo_mode='new'"
