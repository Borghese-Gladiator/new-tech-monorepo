---
title: Land Z scope — forward-fix in cmd_complete + doctor check + module docstrings
motivation: Y's read-layer carve-out fixes the visible `list`/`board` disagreement, but the underlying master-side `metadata.yaml` is still left stale on disk by every future self-modifying `/complete`. The carve-out is a workaround; Z is the root fix. Without Z, the reconciliation script has to be re-run periodically (or invoked per-run via `--run-id`) for every new self-modifying complete, and the disk-state mismatch remains as a footgun for any future reader that bypasses the runs.py enumerators (e.g., `git show master:runs/<id>/metadata.yaml`, a new tool, a future script). The Y carve-out also has a small per-call cost (one extra YAML parse per terminal-state worktree hit in `_walk_worktrees`) that Z eliminates by making the master-side metadata already-correct.
suggested_scope: In `lib/cli/cmd_complete.py` self-modifying path, after `_do_merge()` + `metadata.update()`, add a follow-on commit on `parent_branch` (in the workbench root checkout) that stages and commits the master-side `runs/<id>/metadata.yaml` rewrite. Commit message: `metadata: backfill done status for <run_id>`. Wrap in narrow `try: except (subprocess.CalledProcessError, OSError)` — on caught failure, emit `MasterMetadataBackfillFailed` event to `events.jsonl`, log a one-line WARN to stderr, proceed with worktree removal. Add a `cmd_doctor.py` advisory check: walk master-side `runs/<id>/metadata.yaml`, flag any non-terminal status whose `agent/<slug>` branch is already merged into `parent_branch`. One-line WARN per offender. Doctor does NOT auto-fix. Add module docstrings to `lib/runs.py` (near `_walk_worktrees`) and `lib/cli/cmd_complete.py` documenting the invariant ("after /complete returns, master-side metadata MUST be terminal"). Test: a new test class in `tests/test_self_modifying.py` driving `/complete` end-to-end and asserting `git show <root_branch>:runs/<id>/metadata.yaml | grep "^status:"` reports `done`. **Pre-implementation:** verify which working directory `repos.merge_no_ff` ends up in after the merge (worktree vs. workbench root). The follow-on commit's `git -C` target depends on this.
category: scope_extension
---

Z is fully captured in plan.md's `## Deferred to follow-ups` section. The user explicitly re-scoped this run to Y mid-planning (recorded in brief.md's `## Scope reduction` block) with the intent of Z landing as a follow-up. Y's `_walk_worktrees` carve-out plus the one-shot reconciliation script close the immediate symptom; Z is the durable structural fix.

---
title: Confirm or rule out the same master-vs-worktree write asymmetry in cmd_abandon
motivation: `cmd_complete`'s bug shape (worktree-side metadata write happens AFTER the merge commit, leaving master stale) could plausibly exist in `cmd_abandon` too. If a self-modifying run is `/abandon`-ed, the abandoned-side `metadata.yaml` write might happen after the worktree's commit but before any merge-onto-master, with the same outcome. Today no one has reported the symptom — but the brief explicitly Non-goals'd this and the audit is worth doing once Z's pattern is established. If the bug shape DOES exist, the fix is straightforward (apply Z's follow-on-commit pattern to `cmd_abandon`); if it doesn't (e.g., `cmd_abandon` writes metadata BEFORE the merge, or doesn't merge at all), document why and close.
suggested_scope: Read `lib/cli/cmd_abandon.py` and trace the merge + metadata-write sequence for the self-modifying path. Build a synthetic test case: abandon a self-modifying run, inspect master-side `metadata.yaml`, assert `status: abandoned` is correctly reflected. If the test fails, apply Z's fix pattern (follow-on commit on parent_branch). If it passes, document the difference in `cmd_abandon.py`'s module docstring so future readers don't have to re-do the trace. Add the equivalent doctor check (non-terminal-but-merged for `abandoned` runs). Out of scope: refactoring `cmd_abandon`'s general structure.
category: bug_risk
---

---
title: Audit list vs board collision-resolution asymmetry generally
motivation: Y's carve-out makes the two enumerators agree for the *terminal-state* collision case (master stale, worktree done). But `iter_all_runs` (used by `list`) and `_walk_worktrees` (used by `board` via `iter_all_runs`) still apply DIFFERENT preferences for collisions in general. `iter_all_runs` prefers worktree on collision and warns to stderr; `_walk_worktrees` has the terminal-state filter, the recorded-worktree-path-match filter, and now the stale-master carve-out. Other collision cases (e.g., a run with two live worktrees, or a master-side hit that's newer than the worktree-side hit) may still produce silent disagreement. Worth a once-over to map every collision case and document the resolution choice — or, better, factor a shared resolution helper so future changes can't drift.
suggested_scope: Read `iter_all_runs`, `_walk_worktrees`, and `find_run` in `lib/runs.py`. Build a decision table of "collision shape → resolution choice → which enumerator(s) apply it." For any case where list and board would resolve differently, either (a) document why the difference is intentional, or (b) factor a shared helper and use it from both. Add unit tests for the documented cases. Out of scope: changing the public API of any enumerator — this is structural cleanup, not behavior change.
category: refactor
---

---
title: Fix 7 pre-existing test failures in test_backfill_base_ref_sha.py and test_human_review.py
motivation: The full test suite reports 7 failures on the pristine `master` ref (verified during this run's build phase by stashing all changes and re-running). 5 are in `tests/test_backfill_base_ref_sha.py` (likely related to the previous backfill tooling having drifted from its tested behavior) and 2 are snapshot tests in `tests/test_human_review.py` (`TestSnapshotRender::test_bounce_pass2_snapshot`, `TestSnapshotRender::test_happy_snapshot`). These were NOT caused by this run, but they degrade the signal-to-noise ratio of every `pytest tests/` invocation and make it harder for future builders to notice NEW regressions. Worth fixing or updating snapshots in a dedicated maintenance run.
suggested_scope: Investigate why each of the 7 tests fails today. For the snapshot tests, regenerate against current renderer output (if the renderer is correct and the snapshot is stale). For the backfill tests, either fix the test fixtures to match current `tools/backfill_base_ref_sha.py` behavior or fix the tool itself if the tests caught a real regression. Aim for `pytest tests/` to report 0 failures on master.
category: tech_debt
---

---
title: Add `/build` slash command — close the building-stage curated-entry enforcement gap
motivation: `/shape`, `/plan`, `/validate`, `/followups` each have a slash command that wraps the corresponding stage's `--init` (curated-entry staging) and finalize (transition + evidence) steps. Building is the only LLM-bearing stage without a slash command — the build proceeds without an LLM prompt that says "read `build-context.md` first." During this run, the build-context.md curated file was generated by `cmd_start` at `ready -> building`, but enforcement that the builder reads it FIRST is convention-strength only. Adding `/build` closes the asymmetry and also enables a bounce-rebuild path (`/build --init` regenerates `build-context.md` after a bounce, which today requires re-running `/start` — see F-004 from the 2026-05-25 `generalize-stage-context-md` run). This is TODO §3 in the repo's docs/TODO.md but is also surfaced naturally by THIS run noticing the asymmetry.
suggested_scope: Mirror `cmd_validate.py --init` / `cmd_validate.py` shape: `/build <id> --init` regenerates `build-context.md` (calls existing `cmd_start._write_build_context_artifacts` extracted into a public helper, OR a thin wrapper); `/build <id>` verifies `build.md` exists + non-empty and transitions `building -> validating` (today this is done by `validate --init`). Write `.claude/commands/build.md` with Step 2 mirroring `validate.md`'s "Do NOT re-read X if `<stage>-context.md` covers what you need." Update `tests/test_e2e.py::TestE2EHappyPath::test_happy_path` to drive through `/build --init` and `/build` finalize. Document in `docs/lifecycle.md` § building.
category: scope_extension
---
