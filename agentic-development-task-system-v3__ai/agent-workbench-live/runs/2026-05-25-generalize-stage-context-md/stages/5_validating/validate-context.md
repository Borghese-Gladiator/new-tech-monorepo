# validate-context.md
<!-- Generated by `validate --init`. Read this instead of brief/plan/build/qa separately. Pass-2 (B2). -->
## Original task
Generalize the `*-context.md` pattern that today exists only for the `validate` stage to every LLM-bearing stage (`shape`, `plan`, `build`, `followups`). Each `--init` step writes a deterministic `<stage>-context.md` containing exactly what the next stage needs, filtered from prior artifacts. The agent reads that one curated file at stage entry instead of multiple raw artifacts. Two payoffs: a smaller master-session prefix (cache discipline), and a self-contained file that's the natural input for an Agent-tool subagent.

This run lands the highest-leverage piece first — `build-context.md` — and is scoped to that one builder plus its tests, wiring, and lifecycle-doc row. The other three builders (`plan-context.md`, `followups-context.md`, `shape-context.md`) are explicit non-goals for this run; they're follow-ups.

### User-facing behavior
The agent operating a run sees one new file appear in `runs/<id>/build-context.md` once `/start` succeeds. The `/build` slash-command instructions point the agent at that file first; the agent reads it and is expected to skip re-reading `brief.md` and `plan.md` unless something the curated file flags as "see anchor in brief.md#X" sends it deeper.

There is no new slash command. There is no new CLI subcommand. The existing `/start` transition does one additional deterministic write before the worktree is handed back to the user. The board, audit log, and metrics writers are unchanged — `build-context.md` is just another artifact in the run directory.

Failure mode: if `build-context.md` cannot be generated (e.g. malformed `brief.md` or `plan.md`), the `start` transition still succeeds and a sentinel file is written explaining what was missing. This mirrors the convenience-artifact swallow-and-continue pattern used today by `_write_validate_context_artifacts` (cmd_validate.py:82-84). The state machine never blocks on a context-file generation failure.

## Acceptance criteria
1. A function `build_context.build(meta, brief_text, plan_text) -> str` exists in `agent-workbench-live/lib/build_context.py`, mirroring the shape of `lib/validate_context.py`.
2. `cmd_start.py` calls that builder after the worktree is created and writes the result to `runs/<id>/build-context.md` inside the worktree's run directory (per the per-worktree-run-dir convention from the prior run).
3. The generated file contains, in order: Acceptance criteria + Non-goals (lifted from `brief.md`), Proposed changes + Files likely to change + Test plan + Definition of done (lifted from `plan.md`), Filtered Decisions & assumptions (from `plan.md#decisions--assumptions` if present), Worktree path + branch name + base_ref_sha (lifted from metadata), and the `build.md` template skeleton, and a rules-reminder block ("stay bounded by brief, record deviations in `build.md`").
4. Each section contains anchor links back to the full source artifact so an agent that needs more context knows exactly where to look (mirroring `validate-context.md`'s anchor pattern).
5. `agent-workbench-live/.claude/commands/handoff.md` and/or whichever command opens the building stage is updated so step 1 reads `build-context.md` first, with language mirroring `validate.md` step 2: "Do NOT re-read `brief.md` / `plan.md` if `build-context.md` already covers what you need."
6. A unit test file `tests/test_build_context.py` exists, mirroring `tests/test_validate_context.py`'s shape: feed synthetic `brief.md` + `plan.md` + `metadata.yaml`; assert the generated context has all the expected sections, the anchor links resolve, and the sentinel-fallback path triggers when prior artifacts are malformed.
7. `docs/lifecycle.md` gains a `build-context.md` row in the `building` stage's table, sibling to "Reads" and "Produces."
8. If `build-context.md` generation raises, the `start` transition still succeeds and a sentinel file is written (test coverage: monkey-patch builder to raise, assert transition still succeeds and sentinel file is on disk).
9. No regression in existing tests. The full pytest suite passes.

## Plan decisions + assumptions (filtered)
### DR-001
- **Decision**: `build_context.build` takes `pathlib.Path` arguments and reads files internally, matching `validate_context.build`'s signature shape. Not a pure string-in function.
- **Rationale**: Consistency with the existing curated-context builder. The brief's "pure function takes strings" framing was an assumption in the raw idea; `validate_context.build` is explicitly path-in, and `_read()` is the load-bearing defensive helper. Mirroring keeps cognitive overhead minimal and avoids the question "why does this builder take strings when the other takes paths?"
- **Alternatives considered**: (a) Pure string-in (raw idea's framing); (b) Mixed — paths for big artifacts, strings for `meta`.
- **Why not the alternatives**: (a) Introduces an inconsistency that hurts the next person reading both files. (b) is what this plan adopts for `meta` specifically — it's a dict, not a file — so the "mixed" critique evaporates; the substantive choice is "paths for the markdown artifacts," which matches `validate_context`.

### DR-002
- **Decision**: Write `build-context.md` from `cmd_start.py`, not from a hypothetical `/build` slash command's `--init` step or from a building-stage entry hook.
- **Rationale**: The raw idea floated both options. The brief committed to `/start`. Validating that choice during planning: there is no `/build` slash command at all (subagent investigation confirmed). The building stage is entered passively by the agent operating inside the worktree after `/start`. So `/start` is the only deterministic write point upstream of building.
- **Alternatives considered**: Write it on first agent-side action (e.g. lazy generation when the agent first reads the file); add a new `/build --init` slash command to symmetrize with `/shape --init` / `/plan --init` / `/validate --init`.
- **Why not the alternatives**: Lazy generation moves a deterministic step into the agent's responsibility — exactly what curated-context exists to prevent. Adding a new slash command is scope creep against the brief's non-goal list and adds friction for the human (one more command to remember).

### DR-003
- **Decision**: Duplicate the helper functions `_read`, `_section`, `_HEADING_RE` (and a small variant of the DR/ASM block extractor) into `lib/build_context.py` rather than importing from `lib/validate_context.py` or extracting a shared `lib/context_helpers.py`.
- **Rationale**: The two builders may diverge later (different sources, different filters, different output structures — followups-context.md, plan-context.md, shape-context.md will each have their own quirks). A shared base prematurely couples them. The bad-examples block in the brief explicitly flags "edits to validate_context.py itself" as a bad outcome; duplicating instead of refactoring honors that. The functions are 5–10 lines each; the duplication cost is trivial.
- **Alternatives considered**: Extract a shared `lib/markdown_sections.py` with `read`, `section`, `heading_re` exported; import from validate_context directly.
- **Why not the alternatives**: Extracting now means a third file to read and a shared API surface to maintain; the extraction is cheaper *after* the third builder lands (when the right shape is visible). Importing from validate_context creates a sibling-import cycle in a flat lib/ layout for what should be a small, self-contained file.

### DR-004
- **Decision**: Include all DR-NNN / ASM-NNN blocks from `plan.md` in `build-context.md#decisions--assumptions`, without filtering against `build.md` (which doesn't yet exist at building-entry time).
- **Rationale**: At validate-time, `_filtered_plan_blocks` filters DR/ASM blocks against `build.md` so the reviewer sees only the decisions actually referenced. At building-entry there's no `build.md` yet, so there's nothing to filter against; the agent needs the full list to keep itself bounded. The "filtered" label in the brief was a copy-paste error; corrected here to "all relevant" in the actual heading.
- **Alternatives considered**: Filter against `brief.md` (which decisions does the brief mention?); leave the section empty and tell the agent to read `plan.md` if they want decisions.
- **Why not the alternatives**: Filtering against brief is noise — briefs typically don't mention DR-NNN IDs because the brief is code-blind. Leaving empty defeats the cache-discipline goal (agent still has to read plan.md).

### ASM-001
- **Text**: `meta` passed into `cmd_start.run` is stale after `metadata.update` on line 88 (the closure mutates the on-disk file but the local dict isn't refreshed). `_write_build_context_artifacts` must reload meta via `metadata.load(cfg, run_id)` if it needs the `base_ref_sha`.
- **Reason**: Reading `cmd_start.py`, the `_m` closure at line 84 mutates a dict argument passed to `metadata.update`. Standard `metadata.update` patterns I've seen elsewhere in this codebase read-modify-write the file; whether they also mutate the caller's `meta` reference depends on `metadata.update`'s implementation. Safer to reload than to trust.
- **Impact**: low. Worst case: `base_ref_sha` is missing from `build-context.md#worktree` for the first run that hits the helper. Easy to spot and fix.

### ASM-002
- **Text**: Running `python -m pytest` from `agent-workbench-live/` works without a virtualenv shim or `bin/pytest` invocation. If a shim is required (analogous to `bin/pytest` patterns elsewhere in this repo family), the builder discovers it during the build stage and records the actual command in `build.md`.
- **Reason**: The user's CLAUDE.md mentions `bin/pytest` for the `app` (Django) repo. This is a different repo (no Django). The standard pattern for `agent-workbench-live` is bare `python -m pytest`; confirming during build.
- **Impact**: medium. Wrong invocation means the test suite isn't actually validated. Easy to catch (commands log will show the failure).

### ASM-003
- **Text**: `templates/build.md` exists at `agent-workbench-live/templates/build.md` with sensible section headings that can be inlined verbatim into `build-context.md`.
- **Reason**: The subagent listed `templates/build.md` in its summary as a real file. The exact contents weren't quoted, but the file's existence is enough to plan against. If the contents turn out to be unsuitable (e.g. heavy boilerplate that bloats `build-context.md`), the build stage either trims at inline time or files a follow-up.
- **Impact**: low. Worst case: `build-context.md` has a verbose inline template section; cheap to iterate.

### ASM-004
- **Text**: `docs/lifecycle.md`'s `building` stage table has a structure compatible with adding a new `build-context.md` row alongside existing rows (Reads, Produces, etc.). The subagent reported a table-with-named-rows shape; this assumption holds unless the file has changed since.
- **Reason**: Lifecycle docs are slow-changing; the subagent read the file during planning.
- **Impact**: low. If the table shape is different, the builder adjusts in-place; the change is small.


## Final diff
```
```

### Uncommitted (worktree vs HEAD)
```
 .../agent-workbench-live/AGENTS.md                 |   1 +
 .../agent-workbench-live/lib/cli/cmd_start.py      |  44 +++++-
 .../agent-workbench-live/lib/cli/cmd_validate.py   |   5 +
 .../agent-workbench-live/lib/validate_context.py   | 154 ++++++++++++++++++---
 .../agent-workbench-live/tests/test_e2e.py         |  30 ++++
 .../tests/test_validate_context_build.py           |  86 ++++++++++++
 .../docs/lifecycle.md                              |   7 +-
 7 files changed, 306 insertions(+), 21 deletions(-)
```

(full uncommitted diff is 512 lines; exceeds 500-line cap. See `Files changed` below.)

## Files changed

**Uncommitted** (worktree vs `HEAD`):
```
M	agentic-development-task-system-v3__ai/agent-workbench-live/AGENTS.md
M	agentic-development-task-system-v3__ai/agent-workbench-live/lib/cli/cmd_start.py
M	agentic-development-task-system-v3__ai/agent-workbench-live/lib/cli/cmd_validate.py
M	agentic-development-task-system-v3__ai/agent-workbench-live/lib/validate_context.py
M	agentic-development-task-system-v3__ai/agent-workbench-live/tests/test_e2e.py
M	agentic-development-task-system-v3__ai/agent-workbench-live/tests/test_validate_context_build.py
M	agentic-development-task-system-v3__ai/docs/lifecycle.md
```

**Untracked** (not yet `git add`-ed):
```
agentic-development-task-system-v3__ai/agent-workbench-live/lib/build_context.py
agentic-development-task-system-v3__ai/agent-workbench-live/runs/2026-05-25-generalize-stage-context-md/HUMAN_REVIEW.md
agentic-development-task-system-v3__ai/agent-workbench-live/runs/2026-05-25-generalize-stage-context-md/archive/4_building/build-v1.md
agentic-development-task-system-v3__ai/agent-workbench-live/runs/2026-05-25-generalize-stage-context-md/archive/5_validating/blast-radius-v1.txt
agentic-development-task-system-v3__ai/agent-workbench-live/runs/2026-05-25-generalize-stage-context-md/archive/5_validating/qa-v1/artifacts/pytest_full.log
agentic-development-task-system-v3__ai/agent-workbench-live/runs/2026-05-25-generalize-stage-context-md/archive/5_validating/qa-v1/artifacts/test_build_context.log
agentic-development-task-system-v3__ai/agent-workbench-live/runs/2026-05-25-generalize-stage-context-md/archive/5_validating/qa-v1/commands.txt
agentic-development-task-system-v3__ai/agent-workbench-live/runs/2026-05-25-generalize-stage-context-md/archive/5_validating/qa-v1/report.md
agentic-development-task-system-v3__ai/agent-workbench-live/runs/2026-05-25-generalize-stage-context-md/archive/5_validating/review-v1.md
agentic-development-task-system-v3__ai/agent-workbench-live/runs/2026-05-25-generalize-stage-context-md/archive/5_validating/validate-context-v1.md
agentic-development-task-system-v3__ai/agent-workbench-live/runs/2026-05-25-generalize-stage-context-md/archive/6_followups/follow-ups-v1.md
agentic-development-task-system-v3__ai/agent-workbench-live/runs/2026-05-25-generalize-stage-context-md/audit.md
agentic-development-task-system-v3__ai/agent-workbench-live/runs/2026-05-25-generalize-stage-context-md/change-request.md
agentic-development-task-system-v3__ai/agent-workbench-live/runs/2026-05-25-generalize-stage-context-md/events.jsonl
agentic-development-task-system-v3__ai/agent-workbench-live/runs/2026-05-25-generalize-stage-context-md/metadata.yaml
agentic-development-task-system-v3__ai/agent-workbench-live/runs/2026-05-25-generalize-stage-context-md/metrics.jsonl
agentic-development-task-system-v3__ai/agent-workbench-live/runs/2026-05-25-generalize-stage-context-md/qa/commands.txt
agentic-development-task-system-v3__ai/agent-workbench-live/runs/2026-05-25-generalize-stage-context-md/qa/report.md
agentic-development-task-system-v3__ai/agent-workbench-live/runs/2026-05-25-generalize-stage-context-md/review.md
agentic-development-task-system-v3__ai/agent-workbench-live/runs/2026-05-25-generalize-stage-context-md/stages/1_draft/raw-idea.md
agentic-development-task-system-v3__ai/agent-workbench-live/runs/2026-05-25-generalize-stage-context-md/stages/2_shaping/brief.md
agentic-development-task-system-v3__ai/agent-workbench-live/runs/2026-05-25-generalize-stage-context-md/stages/3_planning/plan.md
agentic-development-task-system-v3__ai/agent-workbench-live/runs/2026-05-25-generalize-stage-context-md/stages/4_building/build.md
agentic-development-task-system-v3__ai/agent-workbench-live/runs/2026-05-25-generalize-stage-context-md/stages/5_validating/blast-radius.txt
agentic-development-task-system-v3__ai/agent-workbench-live/runs/2026-05-25-generalize-stage-context-md/stages/5_validating/validate-context.md
agentic-development-task-system-v3__ai/agent-workbench-live/tests/test_build_context.py
```

## Commands run
(none recorded in build.md)

## Test results
### Unit tests

### Integration tests

### Lint / typecheck

### Browser / Playwright

### Smoke scripts

## Known issues / risks
(none recorded in build.md)

## Reviewer reading order
(no explicit `## Reviewer reading order` in build.md — defaults:)

1. brief.md → Acceptance criteria — confirm scope.
2. plan.md → Decisions & assumptions — confirm load-bearing trade-offs.
3. build.md → Implementation summary + Files changed.
4. review.md (this stage authors it) — adversarial findings.
5. qa/report.md — what ran, what passed, what failed.
6. stages/5_validating/blast-radius.txt — callers depth 1-3.
