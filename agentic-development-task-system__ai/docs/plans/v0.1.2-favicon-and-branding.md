# v0.1.2 — Favicon & App Branding

## Brief

Incorporate the custom robot logo as the app favicon and top-nav logo. Rename the app from "Agentic-Driven Development" to "Agent Task System" across all surfaces (HTML title, manifest, nav bar).

---

## Changes

1. **Static assets** — Created `public/` directory, copied all favicon assets from `favicon_io/` (favicon.ico, 16x16, 32x32, apple-touch-icon, android-chrome 192/512, site.webmanifest)
2. **`src/client/index.html`** — Added favicon, apple-touch-icon, and manifest `<link>` tags; updated `<title>` to "Agent Task System"
3. **`public/site.webmanifest`** — Set `name` and `short_name` to "Agent Task System"
4. **`src/client/App.tsx`** — Replaced `<Zap>` lucide icon with `<img>` of the 32x32 favicon; renamed header text; removed unused `Zap` import
