# PR Summary: app_116708

- **Title**: [CORE-587] Funnel Channel Summary - backend pre-migration logic
- **Author**: timothysheee
- **State**: OPEN
- **URL**: https://github.com/klaviyo/app/pull/116708
- **Created**: 2026-05-04T15:46:28Z
- **Closed**: n/a
- **Merged**: n/a
- **Files changed**: 6  (+577 / -26)
- **Review decision**: REVIEW_REQUIRED
- **Labels**: 

## PR Description

[CORE-587 - [SCF] Migrate to multi-channel funnel summary widget](https://linear.app/klaviyo/issue/CORE-587)

# Description
Backend pre-migration logic for Funnel Summary Chart widget. In the future, we will replace all Email,SMS,Push funnel summary widgets with a single multi-channeled widget. This PR uses a feature flag to control what charts are visible, so we can proceed with the migration

### Changes Overview
- `src/learning/app/custom_analytics/dashboard_service.py`
  - check Statsig gate `reporting_enable_multi_channel_funnel_widget`
  - add `get_visible_widget_count` service method returning `max` visible count across gate states for the frontend dashboard-load check
- `src/learning/app/custom_analytics/views/performance_dashboards.py`
  - add endpoint `visible_widget_count` action on `WidgetViewSet` mapped to `GET /ajax/performance-dashboard/{pk}/visible-widget-count`
- `src/learning/_pattern_owners_generated.py` - regenerated to register the new `visible-widget-count` URL pattern under the `reporting` owner
- `tests/custom_analytics/test_dashboard_service.py`
- `tests/custom_analytics/views/test_performance_dashboards.py`

# Manual Test Plan
[Testing Plan GDoc](https://docs.google.com/document/d/1OtP7GorEQPq4utGIOTd66shvlq66OWA72cmLuWllw8s/edit?tab=t.ghx09rxseaho#heading=h.ypssrpvigah4)

- validate adding widgets with gate on/off locally
- validate adding widgets with gate on/off in webserver

<details>
<summary><h1>Checklist</h1></summary>

Authors and Reviewers, please ensure the following:

## General

- [X] This PR has fewer than 500 lines of code changed
- [X] This PR does not contain a mix of functional and non-functional changes
- [X] If the reviewer isn't entirely familiar with the context of the change, an SME has been tagged

## Tests

- [X] Each functional change has corresponding test cases that have been added/modified
- [X] Edge cases have been considered and tested
- [X] Mocked data makes sense with what is being tested

# Risk Impact

Ensure that these changes are not going to cause any issues in production. Consider the following:

- What are the downstream impacts of this change? Are there any datastore concerns? => **Low. The Statsig gate is OFF by default in every environment, so visibility behavior is unchanged at merge time. The migration command is dry-run by default and idempotent; it only adds rows for dashboards that don't already have a BY_CHANNEL funnel widget. Datastore impact is bounded by the count of dashboards with at least one per-channel funnel widget; insertion happens via `create_widget` (existing path) and is keyset-paginated.**
- Are there any performance implications? => **One additional Statsig gate check per `get_widgets` / `refresh_dashboard_if_old` / `create_widget` / `trigger_dashboard_update_task` call. The gate evaluator is in-process and short-circuits on error. The new `visible_widget_count` endpoint runs the same `get_widgets` query already used by dashboard load. The migration command uses keyset pagination (`performance_dashboard_id__gt=last_id`) to avoid full-table scans on the 37M-row Widget table.**
- Are there environment-specific flags that would lead to behavioral differences between runtimes/environments? => **Yes by design — `reporting_enable_multi_channel_funnel_widget` controls visibility per-company via Statsig. Default is OFF everywhere; ramped per the rollout plan in the Linear ticket.**
- Is this change forward compatible (with data structures produced by previous versions)? => **Yes. The legacy per-channel `FUNNEL_SUMMARY/{EMAIL,SMS,PUSH}` rows remain unchanged in the DB and continue to be served when the gate is OFF. The new BY_CHANNEL row is additive.**
- Is this change backwards compatible if a revert is needed? => **Yes. Rollback = disable the Statsig gate; legacy rows immediately reappear. Any BY_CHANNEL rows added by the migration are left in the DB (hidden when gate is OFF) — no code revert required.**
- How will you monitor the change?  => **Sentry for exceptions in `PerformanceDashboardService` (gate-check failures already log + fail closed). Datadog for the celery `process_funnel_summary_by_channel` task latency / queue depth. Migration command logs per-batch progress + final tally to stdout. During ramp: dashboard-load p95, widget render error rate, and a canary alert on dashboards rendering zero funnel widgets.**

# Deploy Plan

<!-- Replace the default plan below if necessary -->

Everywhere (excluding the hosts that are not part of `qw-ip`)

```sh
# Run these commands, replacing the placeholder with the built artifact ID from Jenkins
klaviyocli deploy app deploy PASTE_THE_ARTIFACT_ID_HERE webservers
klaviyocli deploy app deploy PASTE_THE_ARTIFACT_ID_HERE qw-ip
```

______________________________________________________________________

- [Author Guidelines](https://klaviyo.atlassian.net/wiki/spaces/EN/pages/2917630292/How+to+Submit+Pull+Requests)
- [Reviewer Guidelines](https://klaviyo.atlassian.net/wiki/spaces/EN/pages/2921070779/How+to+Review+Code)
- Feedback on this template, reach out to [#code-review-wrkgrp](https://klaviyo.enterprise.slack.com/archives/C07F4SFQ3A7)

<!--
Please read and follow the guidelines.

Always Write Something(TM)... even in PR descriptions. It's rubber-duck-debugging for you and
it's a courtesy for your fellow engineers.
-->

</details>


---

## Reviews (top-level review submissions)

### Review by @cursor[bot] — COMMENTED — 2026-05-04T15:52:38Z



### Review by @cursor[bot] — COMMENTED — 2026-05-04T17:21:20Z



### Review by @timothysheee — COMMENTED — 2026-05-04T18:55:46Z



### Review by @timothysheee — COMMENTED — 2026-05-04T19:46:46Z



### Review by @danieladukeshire — COMMENTED — 2026-05-06T19:00:13Z



### Review by @danieladukeshire — COMMENTED — 2026-05-06T19:00:29Z



### Review by @danieladukeshire — COMMENTED — 2026-05-06T19:03:25Z



### Review by @danieladukeshire — COMMENTED — 2026-05-06T19:04:15Z



### Review by @danieladukeshire — COMMENTED — 2026-05-06T19:20:53Z



### Review by @danieladukeshire — COMMENTED — 2026-05-06T19:24:01Z



### Review by @cursor[bot] — COMMENTED — 2026-05-07T14:06:56Z



### Review by @gillian-palmer — COMMENTED — 2026-05-07T14:50:08Z

Looks good, my main note is that the comment blocks should be reduced as they can be a bit redundant.

### Review by @gillian-palmer — COMMENTED — 2026-05-07T14:50:31Z



### Review by @timothysheee — COMMENTED — 2026-05-07T15:44:59Z



### Review by @timothysheee — COMMENTED — 2026-05-07T15:45:00Z



### Review by @timothysheee — COMMENTED — 2026-05-07T15:45:03Z



### Review by @timothysheee — COMMENTED — 2026-05-07T15:45:04Z



### Review by @timothysheee — COMMENTED — 2026-05-07T15:45:06Z



### Review by @timothysheee — COMMENTED — 2026-05-07T15:45:09Z



### Review by @timothysheee — COMMENTED — 2026-05-07T15:45:16Z



### Review by @timothysheee — COMMENTED — 2026-05-07T15:45:17Z



### Review by @cursor[bot] — COMMENTED — 2026-05-07T15:52:00Z



### Review by @timothysheee — COMMENTED — 2026-05-07T16:34:16Z



### Review by @timothysheee — COMMENTED — 2026-05-07T16:36:43Z



### Review by @cursor[bot] — COMMENTED — 2026-05-07T16:59:54Z

<!-- BUGBOT_REVIEW -->
Cursor Bugbot has reviewed your changes using default mode and found 1 potential issue.



<!-- BUGBOT_FIX_ALL -->
<a href="https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..bip2H9-WsNDa1kgnj3eRJpPdhKPWruQT1tX1TAr0AiwYqgNuWg89VjaGdDdn0uecWzZVz1g8onxmt1tYBj7gGNX7cR4BeQELnCUVwKqewBxmOnt9cxcVDiwpyTgclVw0Kv--Igh3eC2p8SvsHRQuO5VM3cbnN1b6K5hKYvs5Bwau-34mdpof1PbhJzX0GyDqSC4f0eWM0DXmfuk6Fsrt7b4U1o-73rlLb00nFjv02xjxGLCBJ4CPew2wdHy_d-SL4VRg8LcJ_ylISboNyNL9V6aaM1Ee4IH7drmrzQvpBH_qghkWwHoiPZTfmwiWp0jtfegnqrvYkW5fn8XcsYMUsA" target="_blank" rel="noopener noreferrer"><picture><source media="(prefers-color-scheme: dark)" srcset="https://cursor.com/assets/images/fix-in-cursor-dark.png"><source media="(prefers-color-scheme: light)" srcset="https://cursor.com/assets/images/fix-in-cursor-light.png"><img alt="Fix All in Cursor" width="115" height="28" src="https://cursor.com/assets/images/fix-in-cursor-dark.png"></picture></a>
<!-- /BUGBOT_FIX_ALL -->

<!-- BUGBOT_AUTOFIX_REVIEW_FOOTNOTE_BEGIN -->
<sup>❌ Bugbot Autofix is OFF. To automatically fix reported issues with cloud agents, have a team admin enable autofix in the [Cursor dashboard](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- BUGBOT_AUTOFIX_REVIEW_FOOTNOTE_END -->

<sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 048aa5cc136891445aac6430b9a5d6411caca63a. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>

---

## Inline Review Comments (on diff)

### @cursor[bot] on `src/learning/app/management/commands/z20260505_add_funnel_summary_by_channel_widget.py:92` — 2026-05-04T15:52:40Z

```diff
@@ -0,0 +1,172 @@
+"""Post-deploy command for CORE-587: insert a FUNNEL_SUMMARY/BY_CHANNEL widget on
+every dashboard that has at least one per-channel funnel summary widget.
+
+Dry-run by default; pass --execute to write. The new widget is positioned at
+``min(order)`` of the source rows so its slot lines up with the legacy widgets
+(once the legacy rows are deleted in CORE-589 the position is preserved).
+"""
+
+from __future__ import annotations
+
+import logging
+from argparse import ArgumentParser
+from typing import Any
+
+from app.custom_analytics.dashboard_service import PerformanceDashboardService
+from app.custom_analytics.data_services.types.dashboards import (
+    TooManyPerformanceDashboardWidgets,
+    WidgetSubtype,
+    WidgetType,
+)
+from app.management.commands import BaseAppCommand
+from app.models import Widget
+
+logger = logging.getLogger("django")
+
+_SOURCE_SUBTYPES: list[WidgetSubtype] = [
+    WidgetSubtype.EMAIL,
+    WidgetSubtype.SMS,
+    WidgetSubtype.PUSH,
+]
+
+_SUBTYPE_LABEL: dict[WidgetSubtype, str] = {
+    WidgetSubtype.EMAIL: "email",
+    WidgetSubtype.SMS: "sms",
+    WidgetSubtype.PUSH: "push",
+}
+
+
+class Command(BaseAppCommand):
+    help = (
+        "Add a Funnel Summary By Channel widget to every dashboard that has an "
+        "email, SMS, or push funnel summary widget. Dry-run by default; pass "
+        "--execute to write."
+    )
+
+    def add_arguments(self, parser: ArgumentParser) -> None:
+        parser.add_argument(
+            "--execute",
+            action="store_true",
+            help="Write changes; omit for a dry run.",
+        )
+        parser.add_argument(
+            "--batch-size",
+            type=int,
+            default=2000,
+            help="Keyset-pagination batch size (default: 2000).",
+        )
+
+    def run(self, *args: Any, **options: Any) -> None:
+        execute: bool = bool(options.get("execute"))
+        batch_size: int = int(options["batch_size"])
+
+        # Phase 1: keyset-paginate the per-channel funnel rows and collect the
+        # min(order) per (company, dashboard, subtype). One per-channel funnel
+        # widget per dashboard is the norm, but we defensively handle dupes.
+        dashboard_subtype_orders: dict[tuple[str, str], dict[int, int]] = {}
+        last_dashboard_id: str | None = None
+
+        while True:
+            qs = Widget.widget_manager(use_replica=False).filter(
+                type=WidgetType.FUNNEL_SUMMARY,
+                subtype__in=_SOURCE_SUBTYPES,
+            )
+            if last_dashboard_id is not None:
+                qs = qs.filter(performance_dashboard_id__gt=last_dashboard_id)
+            batch = list(
+                qs.order_by("performance_dashboard_id").values(
+                    "company_id",
+                    "performance_dashboard_id",
+                    "subtype",
+                    "order",
+                )[:batch_size]
+            )
+            if not batch:
+                break
+            for row in batch:
+                key = (row["company_id"], row["performance_dashboard_id"])
+                bucket = dashboard_subtype_orders.setdefault(key, {})
+                subtype = row["subtype"]
+                order = row["order"]
+                bucket[subtype] = min(bucket.get(subtype, order), order)
+            last_dashboard_id = batch[-1]["performance_dashboard_id"]
```

### Keyset pagination drops rows at batch boundaries

**Medium Severity**

<!-- DESCRIPTION START -->
The keyset pagination uses `performance_dashboard_id__gt` as the cursor, but a single dashboard can have up to 3 rows (EMAIL, SMS, PUSH). When rows for the same dashboard span a batch boundary, the next batch's `__gt` filter skips the remaining rows for that dashboard. For example, if batch N ends with `dashB/EMAIL`, `last_dashboard_id` is set to `"dashB"`, and the `dashB/SMS` and `dashB/PUSH` rows are silently lost. This causes an incorrect `min_order` for affected dashboards, positioning the new `BY_CHANNEL` widget at the wrong order. The pagination test only seeds one widget per dashboard so it doesn't catch this.
<!-- DESCRIPTION END -->

<!-- BUGBOT_BUG_ID: 2f8e5fb7-2177-4d97-a516-a5b7581c97f8 -->

<!-- LOCATIONS START
src/learning/app/management/commands/z20260505_add_funnel_summary_by_channel_widget.py#L73-L92
LOCATIONS END -->
<div><a href="https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..yP9SSLPvaQb8NRjDXFn0QS4kw__GSnn6FyqyxCPtZlKabuZYfYR29dVyJVc8vCnHXPIVn7PanmuFr4lnkVUamHvNV9XOe78tTnKSKUfPSOzaCekwLQW1H_Zqm_bFWyDZGhfXLUZE2xyD3m_X1buVI2YJ5T8vCL4_H8tLQEwVItGoDLGtnxTpwqEDphMgEtOPDvk-Oq84kbEDkJ299sW9iGFoTvb0n6CiSEiWU4wASFu-nAy9hQEtABOqr8V-QusPj2jB67XVIq5CnJBK-j6-D4kTMtTT4tld4EbFzvXhD_MIxE8_nl1-mcS6o-Jl5ogDugowbALBSHKGC2myFACuxQ" target="_blank" rel="noopener noreferrer"><picture><source media="(prefers-color-scheme: dark)" srcset="https://cursor.com/assets/images/fix-in-cursor-dark.png"><source media="(prefers-color-scheme: light)" srcset="https://cursor.com/assets/images/fix-in-cursor-light.png"><img alt="Fix in Cursor" width="115" height="28" src="https://cursor.com/assets/images/fix-in-cursor-dark.png"></picture></a>&nbsp;<a href="https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..rshurvbcy2yqtVPpfHapBVWBIqfNVVfzlWcAwg4wULTXQXcBaaIk3uS91cGVmNIIcKIZ_SfKsieDv_m6XcP-cgJkE5SP-7io7Fn0VbSXgqun_qV942kTTy4CuyMdcGfLKa65jyX5GfTz8TTjjErt5S6fSGK6Ytoi_lT6cWJQpu2WYb3JXeEEf6LWBq3kGRN8wrNdgqUta6QCx1DQJRw2zHeurpv9RRz35FwhlS9w8KW6Mh5mHtgGq9I1ecSRMrPf0gHuLJcAl5soKP5aow9_WGYfxrkhl8orKElco9AyUNB5iTNwMU1NqemzWfwpOv5UHy_mMKBG8mgiIe5sqpnWPw" target="_blank" rel="noopener noreferrer"><picture><source media="(prefers-color-scheme: dark)" srcset="https://cursor.com/assets/images/fix-in-web-dark.png"><source media="(prefers-color-scheme: light)" srcset="https://cursor.com/assets/images/fix-in-web-light.png"><img alt="Fix in Web" width="99" height="28" src="https://cursor.com/assets/images/fix-in-web-dark.png"></picture></a></div>


<sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 60479c750d9af9c063169f27e9d57f73fd5a14c9. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>


### @cursor[bot] on `src/learning/app/custom_analytics/dashboard_service.py:315` — 2026-05-04T17:21:20Z

```diff
@@ -306,6 +311,24 @@
 WIDGET_TYPES_WITH_DETAILS = frozenset({WidgetType.SINGLE_METRIC})
 
 
+# CORE-587: Multi-channel funnel summary widget gating.
+#
+# The per-channel FUNNEL_SUMMARY widgets (EMAIL/SMS/PUSH) are being replaced by a
+# unified FUNNEL_SUMMARY/BY_CHANNEL widget behind the Statsig gate below. Until the
+# legacy rows are deleted (CORE-589), both shapes coexist in the DB; visibility is
+# controlled here.
+#
+# Companion ticket CORE-586 introduces an analogous gate for DELIVERABILITY widgets.
+# When SCD merges to master, a follow-up should unify these into a generic
+# `_is_widget_hidden(widget, hidden_by_type)` helper. We intentionally keep the
+# funnel-specific code self-contained for now so SCF can ship independently.
+_MULTI_CHANNEL_FUNNEL_SUMMARY_GATE = "reporting_enable_multi_channel_funnel_widget"
```

### Statsig gate name missing "summary" — feature unflaggable

**High Severity**

<!-- DESCRIPTION START -->
The gate name `_MULTI_CHANNEL_FUNNEL_SUMMARY_GATE` is set to `"reporting_enable_multi_channel_funnel_widget"`, but the PR description, deploy plan, and post-deploy monitoring all reference the gate as `"reporting_enable_multi_channel_funnel_summary_widget"` (with "summary" between "funnel" and "widget"). Since the Statsig gate will be created with the name from the deploy plan, `check_gate` will always evaluate against a non-existent gate and return `False`, making it impossible to enable the feature by flipping the gate in Statsig.
<!-- DESCRIPTION END -->

<!-- BUGBOT_BUG_ID: 4c7c7dc7-6329-4c6a-aa58-0f587181031a -->

<!-- LOCATIONS START
src/learning/app/custom_analytics/dashboard_service.py#L324-L325
LOCATIONS END -->
<div><a href="https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..dRfckGJzYH2QqNsvH91lxSK2PKb9HJrwh4Hx8wAs9JQ-NWMyQMbB54MrkevV4OexKfhU-WGECaYC2Nkwtm7T_HNwUONB3dbct5WVzzcXZXcUJruekVSQQEGxoDKYsoKgLQzUXlVp03o08H9NXAApnGBQ553tIqSSsKP3sf1cfAUAOLE74jl4NZZngDpRlLCRnLuANZmh30VaF0zynNyR2ipiER_eViQCPiOPe5PIzeU3BBq97g-oAAMeMP_hTyoLX94m6JDqRUeyiHPmnCkWukRv_V4g36pbj2X8rspIdGRmyJbOz7Ohu0BxE-ucqhD9tTx2U5vgfaPcTG1HrJrRpA" target="_blank" rel="noopener noreferrer"><picture><source media="(prefers-color-scheme: dark)" srcset="https://cursor.com/assets/images/fix-in-cursor-dark.png"><source media="(prefers-color-scheme: light)" srcset="https://cursor.com/assets/images/fix-in-cursor-light.png"><img alt="Fix in Cursor" width="115" height="28" src="https://cursor.com/assets/images/fix-in-cursor-dark.png"></picture></a>&nbsp;<a href="https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..FAODDg7Yk6T_MeMxfqev_vwYXTg_2vZZUmvv5oSgcqltvyBJgnYkDIHZBVhJSj4mPRAx7aQmaxct0hJyoqgcoEWKyPaxP5xgpYTeeRG9CSwcvgOXB7VtLScSeEQByj6ELw3MJAxCPVUal3B6wt99lg0G12JOb54mnF25kqzIaVi7UN5uBk6E-mGw1QuI7BTCEVRlHNFkvdQoCmGxsJApB1z77CvdjZNnv_MGRfI7zqsP6NfNBj3PW9W8QE_8-FrQtXYTrbdZusU94WvrA_LMdRRX_Qn2LvSbYdWzwAE_V65gzNbp290kKlHPp5lQh90dm_tniJDDEeYno1HfTmsdsA" target="_blank" rel="noopener noreferrer"><picture><source media="(prefers-color-scheme: dark)" srcset="https://cursor.com/assets/images/fix-in-web-dark.png"><source media="(prefers-color-scheme: light)" srcset="https://cursor.com/assets/images/fix-in-web-light.png"><img alt="Fix in Web" width="99" height="28" src="https://cursor.com/assets/images/fix-in-web-dark.png"></picture></a></div>


<sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 98f4d08f14563bf114b08aa84a6761c821127989. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>


### @timothysheee on `src/learning/app/custom_analytics/dashboard_service.py:315` — 2026-05-04T18:55:46Z

```diff
@@ -306,6 +311,24 @@
 WIDGET_TYPES_WITH_DETAILS = frozenset({WidgetType.SINGLE_METRIC})
 
 
+# CORE-587: Multi-channel funnel summary widget gating.
+#
+# The per-channel FUNNEL_SUMMARY widgets (EMAIL/SMS/PUSH) are being replaced by a
+# unified FUNNEL_SUMMARY/BY_CHANNEL widget behind the Statsig gate below. Until the
+# legacy rows are deleted (CORE-589), both shapes coexist in the DB; visibility is
+# controlled here.
+#
+# Companion ticket CORE-586 introduces an analogous gate for DELIVERABILITY widgets.
+# When SCD merges to master, a follow-up should unify these into a generic
+# `_is_widget_hidden(widget, hidden_by_type)` helper. We intentionally keep the
+# funnel-specific code self-contained for now so SCF can ship independently.
+_MULTI_CHANNEL_FUNNEL_SUMMARY_GATE = "reporting_enable_multi_channel_funnel_widget"
```

Old comment. Updated PR Description and other to `reporting_enable_multi_channel_funnel_widget`

### @timothysheee on `src/learning/app/management/commands/z20260505_add_funnel_summary_by_channel_widget.py:92` — 2026-05-04T19:46:46Z

```diff
@@ -0,0 +1,172 @@
+"""Post-deploy command for CORE-587: insert a FUNNEL_SUMMARY/BY_CHANNEL widget on
+every dashboard that has at least one per-channel funnel summary widget.
+
+Dry-run by default; pass --execute to write. The new widget is positioned at
+``min(order)`` of the source rows so its slot lines up with the legacy widgets
+(once the legacy rows are deleted in CORE-589 the position is preserved).
+"""
+
+from __future__ import annotations
+
+import logging
+from argparse import ArgumentParser
+from typing import Any
+
+from app.custom_analytics.dashboard_service import PerformanceDashboardService
+from app.custom_analytics.data_services.types.dashboards import (
+    TooManyPerformanceDashboardWidgets,
+    WidgetSubtype,
+    WidgetType,
+)
+from app.management.commands import BaseAppCommand
+from app.models import Widget
+
+logger = logging.getLogger("django")
+
+_SOURCE_SUBTYPES: list[WidgetSubtype] = [
+    WidgetSubtype.EMAIL,
+    WidgetSubtype.SMS,
+    WidgetSubtype.PUSH,
+]
+
+_SUBTYPE_LABEL: dict[WidgetSubtype, str] = {
+    WidgetSubtype.EMAIL: "email",
+    WidgetSubtype.SMS: "sms",
+    WidgetSubtype.PUSH: "push",
+}
+
+
+class Command(BaseAppCommand):
+    help = (
+        "Add a Funnel Summary By Channel widget to every dashboard that has an "
+        "email, SMS, or push funnel summary widget. Dry-run by default; pass "
+        "--execute to write."
+    )
+
+    def add_arguments(self, parser: ArgumentParser) -> None:
+        parser.add_argument(
+            "--execute",
+            action="store_true",
+            help="Write changes; omit for a dry run.",
+        )
+        parser.add_argument(
+            "--batch-size",
+            type=int,
+            default=2000,
+            help="Keyset-pagination batch size (default: 2000).",
+        )
+
+    def run(self, *args: Any, **options: Any) -> None:
+        execute: bool = bool(options.get("execute"))
+        batch_size: int = int(options["batch_size"])
+
+        # Phase 1: keyset-paginate the per-channel funnel rows and collect the
+        # min(order) per (company, dashboard, subtype). One per-channel funnel
+        # widget per dashboard is the norm, but we defensively handle dupes.
+        dashboard_subtype_orders: dict[tuple[str, str], dict[int, int]] = {}
+        last_dashboard_id: str | None = None
+
+        while True:
+            qs = Widget.widget_manager(use_replica=False).filter(
+                type=WidgetType.FUNNEL_SUMMARY,
+                subtype__in=_SOURCE_SUBTYPES,
+            )
+            if last_dashboard_id is not None:
+                qs = qs.filter(performance_dashboard_id__gt=last_dashboard_id)
+            batch = list(
+                qs.order_by("performance_dashboard_id").values(
+                    "company_id",
+                    "performance_dashboard_id",
+                    "subtype",
+                    "order",
+                )[:batch_size]
+            )
+            if not batch:
+                break
+            for row in batch:
+                key = (row["company_id"], row["performance_dashboard_id"])
+                bucket = dashboard_subtype_orders.setdefault(key, {})
+                subtype = row["subtype"]
+                order = row["order"]
+                bucket[subtype] = min(bucket.get(subtype, order), order)
+            last_dashboard_id = batch[-1]["performance_dashboard_id"]
```

[[Claude Code]]

Not a real bug in this PR — the migration command this comment was filed against was removed in commit e5b67a1a ("Remove z20260505 management command in favor of migration"). The file is no longer in the PR diff.

For posterity, on the original code: the keyset cursor (`performance_dashboard_id__gt=last_dashboard_id`) combined with `order_by("performance_dashboard_id")` (no tiebreaker) could indeed split a dashboard's rows across a batch boundary and skip the rest of that dashboard's rows on the next batch. However, the impact is bounded because the aggregation only cares about `min(order)` per dashboard — a surviving row still anchors BY_CHANNEL adjacent to the existing per-channel widgets. Worst case is BY_CHANNEL landing one slot off from the true minimum on dashboards whose multiple per-channel funnel rows happen to straddle a 2000-row boundary. Cosmetic, not a data-integrity issue. Resolving as N/A since the code is gone.

### @danieladukeshire on `src/learning/app/custom_analytics/dashboard_service.py:522` — 2026-05-06T19:00:13Z

```diff
@@ -432,6 +519,41 @@ def get_dashboards(
         """
         return self._data_service.get_dashboards(company_id, dashboard_type)
 
+    def get_total_widget_count(self, company_id: str, dashboard_id: str) -> int:
```

Similar to @gillian-palmer's [comment in her PR](https://github.com/klaviyo/app/pull/116383/changes#r3196766112), I think this should be renamed to properly represent what we are returning here. `visible_count` or something along the lines, because this function is not returning the total widget count for the dashboard. 

### @danieladukeshire on `src/learning/app/custom_analytics/dashboard_service.py:572` — 2026-05-06T19:00:29Z

```diff
@@ -447,7 +569,14 @@ def get_widgets(self, company_id: str, dashboard_id: str) -> Iterator[WidgetData
         """
         if not self._data_service.get_dashboard_or_none(company_id, dashboard_id):
             raise PerformanceDashboardDoesNotExist()
-        return self._data_service.get_widgets(company_id, dashboard_id)
+        # CORE-587: hide per-channel funnel widgets when the multi-channel gate is
```

This comment can be deleted

### @danieladukeshire on `src/learning/app/custom_analytics/dashboard_service.py:451` — 2026-05-06T19:03:25Z

```diff
@@ -370,7 +437,27 @@ def refresh_dashboard_if_old(
         if dashboard.last_run:
             update_if_older_than_time = arrow.now().shift(hours=-hours_threshold)
             if dashboard.last_run >= update_if_older_than_time:
-                return False
+                # CORE-587: Even when last_run is recent, force a refresh if any
+                # *visible* widget is still in LOADING. This handles the case where
+                # the post-deploy migration command seeded a FUNNEL_SUMMARY/BY_CHANNEL
+                # widget (status=LOADING) onto a dashboard whose last_run is recent;
+                # without this, the new widget would render as a perpetual spinner
+                # until the next stale-refresh cycle. Hidden widgets are excluded so
+                # we don't do extra work for widgets the user can't see.
+                funnel_summary_hidden = self._get_hidden_funnel_summary_subtypes(
+                    company_id
+                )
+                visible_loading_exists = any(
+                    widget.status == WidgetStatus.LOADING
```

Im not sure if this was branched off of Gillians PR so forgive me if anything is redundant - 

I am a little nervous about this code, & I understand that this was the original suggestion, but I just want to make sure we think this through fully.

Lets say a performance dashboard widget is stuck in the `LOADING` state. In that case, no matter what, we kick off another widget update task on page load, even if a widget task is truly loading. Maybe it would be better to use `ZERO_DATA` at first glance? Or we create a new WidgetStatus `HIDDEN` or `SKIPPED` or `NOT_RUN` or something?

We also would want this to perform ideally not past the `last_run` period, assuming this code is being merged before we perform the migration. This code only kicks off a task after 8 hours of wait time, therefore being blank effectively for up to 8 hours.

I would sync with @gillian-palmer on this

### @danieladukeshire on `src/learning/app/custom_analytics/dashboard_service.py:873` — 2026-05-06T19:04:15Z

```diff
@@ -741,7 +870,32 @@ def create_widget(
         existing_widgets = list(
             self._data_service.get_widgets(company_id, dashboard_id, False)
         )
-        if len(existing_widgets) >= self._MAX_WIDGETS_PER_DASHBOARD:
+        # CORE-587: Enforce the 10-widget limit against the maximum visible count
```

These massive comment blocks don't really help me as a reader 

### @danieladukeshire on `src/learning/app/custom_analytics/dashboard_service.py:455` — 2026-05-06T19:20:53Z

```diff
@@ -370,7 +437,27 @@ def refresh_dashboard_if_old(
         if dashboard.last_run:
             update_if_older_than_time = arrow.now().shift(hours=-hours_threshold)
             if dashboard.last_run >= update_if_older_than_time:
-                return False
+                # CORE-587: Even when last_run is recent, force a refresh if any
+                # *visible* widget is still in LOADING. This handles the case where
+                # the post-deploy migration command seeded a FUNNEL_SUMMARY/BY_CHANNEL
+                # widget (status=LOADING) onto a dashboard whose last_run is recent;
+                # without this, the new widget would render as a perpetual spinner
+                # until the next stale-refresh cycle. Hidden widgets are excluded so
+                # we don't do extra work for widgets the user can't see.
+                funnel_summary_hidden = self._get_hidden_funnel_summary_subtypes(
+                    company_id
+                )
+                visible_loading_exists = any(
+                    widget.status == WidgetStatus.LOADING
+                    and not self._is_funnel_summary_widget_hidden(
+                        widget, funnel_summary_hidden
+                    )
+                    for widget in self._data_service.get_widgets(
```

arent we able to use the function `self.get_widgets(...)` here ? im curious why we have a few `get_widgets`, and if we can consolidate the use cases or be more explicit 

### @danieladukeshire on `src/learning/app/custom_analytics/dashboard_service.py:439` — 2026-05-06T19:24:01Z

```diff
@@ -370,7 +437,27 @@ def refresh_dashboard_if_old(
         if dashboard.last_run:
             update_if_older_than_time = arrow.now().shift(hours=-hours_threshold)
             if dashboard.last_run >= update_if_older_than_time:
```

We got some serious nested if statements, can we pull these out somehow? 

### @cursor[bot] on `src/learning/app/custom_analytics/dashboard_service.py:462` — 2026-05-07T14:06:56Z

```diff
@@ -358,23 +382,103 @@ def _widget_storage_service(self) -> WidgetStorageService:
     def _statistic_service(self) -> StatisticService:
         return StatisticService()
 
+    def _is_multi_channel_funnel_summary_enabled(self, company_id: str) -> bool:
+        """Check the SCF Statsig gate. Fails closed (returns False) on any error."""
+        if not company_id:
+            return False
+        try:
+            statsig_user = StatsigContext(
+                custom_ids={StatsigUserField.COMPANY_ID: company_id},
+            ).generate_finalized_statsig_user()
+            return bool(
+                statsig_client().check_gate(
+                    statsig_user, _MULTI_CHANNEL_FUNNEL_SUMMARY_GATE
+                )
+            )
+        except Exception as e:
+            with sentry_sdk.push_scope() as scope:
+                scope.set_tag("company_id", company_id)
+                scope.set_tag("gate", _MULTI_CHANNEL_FUNNEL_SUMMARY_GATE)
+                sentry_sdk.capture_exception(e)
+            return False
+
+    def _get_hidden_funnel_summary_subtypes(
+        self, company_id: str
+    ) -> frozenset[WidgetSubtype]:
+        """Return the set of FUNNEL_SUMMARY subtypes hidden for this company."""
+        if self._is_multi_channel_funnel_summary_enabled(company_id):
+            return _FUNNEL_SUMMARY_HIDDEN_WHEN_FLAG_ON
+        return _FUNNEL_SUMMARY_HIDDEN_WHEN_FLAG_OFF
+
+    @staticmethod
+    def _is_funnel_summary_widget_hidden(
+        widget: WidgetData,
+        funnel_summary_hidden: frozenset[WidgetSubtype],
+    ) -> bool:
+        """Funnel-only visibility predicate.
+
+        Note: CORE-586 (SCD) will introduce a deliverability-equivalent. Once both
+        ship, unify into a single `_is_widget_hidden(widget, hidden_by_type)` helper.
+        """
+        return (
+            widget.type == WidgetType.FUNNEL_SUMMARY
+            and widget.subtype in funnel_summary_hidden
+        )
+
     def refresh_dashboard_if_old(
         self,
         company_id: str,
         dashboard: PerformanceDashboardData,
         hours_threshold: int = 8,
     ) -> bool:
         """
-        Returns True if dashbboard is updated, else False
+        Returns True if dashboard should be updated, else False
         """
         if dashboard.last_run:
             update_if_older_than_time = arrow.now().shift(hours=-hours_threshold)
             if dashboard.last_run >= update_if_older_than_time:
-                return False
+                # CORE-587: On a non-stale dashboard, fan out per-widget refreshes
+                # for any *visible* widget marked NOT_RUN (the post-deploy migration
+                # seeds new FUNNEL_SUMMARY/BY_CHANNEL widgets at NOT_RUN). Avoids
+                # the full dashboard refresh path, which would flash already-COMPLETE
+                # widgets back to LOADING on every browser refresh. NOT_RUN is
+                # distinct from LOADING so we don't double-trigger widgets that
+                # already have a task in flight.
+                not_run_widgets = [
+                    w
+                    for w in self._visible_widgets(company_id, dashboard.dashboard_id)
+                    if w.status == WidgetStatus.NOT_RUN
+                ]
+                if not not_run_widgets:
+                    return False
+                triggered = False
+                for widget in not_run_widgets:
+                    try:
+                        self.trigger_widget_update_task(
+                            widget, ignore_concurrency_lock=True
+                        )
+                        triggered = True
+                    except UnableToObtainConcurrencyLockException:
+                        continue
```

### Dead exception handler masks unhandled operational errors

**Medium Severity**

<!-- DESCRIPTION START -->
The `except UnableToObtainConcurrencyLockException` handler can never fire because `trigger_widget_update_task` is called with `ignore_concurrency_lock=True`, which skips the lock-acquisition path entirely — the only place that exception is raised. If an actual operational error occurs inside `trigger_widget_update_task` (e.g., Celery is down or a DB write fails), the internal `except Exception` re-raises the original exception, which won't match `UnableToObtainConcurrencyLockException`, so it propagates unhandled up to the `retrieve` view and results in a 500. Since this runs on every non-stale dashboard load with `NOT_RUN` widgets, a transient infrastructure issue would block users from viewing their dashboard until the `NOT_RUN` widgets are successfully processed.
<!-- DESCRIPTION END -->

<!-- BUGBOT_BUG_ID: b06a981f-28c0-46df-ab2b-769503a4b973 -->

<!-- LOCATIONS START
src/learning/app/custom_analytics/dashboard_service.py#L454-L462
LOCATIONS END -->
<div><a href="https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..t4rHjYKDZJdFNtT8mEFB5Yv5GgB8_-6_XaRz6ZSQS0NHobNDhIplGQbUGSGLqiPZ7Q6JGlv5K_BfLChJ--aK9nFXG02dKxRRZ40JJamZOvF4iKkI5BpkKJFOosUOwgeUNh9oOjn5V0NaiWrsd31hZ11PcZMAT-o1Cd3cNBjafWX3UkEOsipFp5dZmdmo0GAYc8GOWfDv645WZpcB09fem9NvEN53YZtlU-LxwosjYM5mxEYYJjbV_MJdf8lHCU708RqOjsHGn7XKqSctO68lfkouX-PpKL6YeKzHdpw7qLevjSyZTajY_nX4TCbw2e7y-bBkonYhn7Th7TevKlWlrQ" target="_blank" rel="noopener noreferrer"><picture><source media="(prefers-color-scheme: dark)" srcset="https://cursor.com/assets/images/fix-in-cursor-dark.png"><source media="(prefers-color-scheme: light)" srcset="https://cursor.com/assets/images/fix-in-cursor-light.png"><img alt="Fix in Cursor" width="115" height="28" src="https://cursor.com/assets/images/fix-in-cursor-dark.png"></picture></a>&nbsp;<a href="https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..kCMPlwWnUyl4d-o3zzSqRb3V6Xna6F-1fOap0kUHMH1iCYc5nDnIhQQcSah5fXVATS3TMLL3wCFEyioEB75beCXDVt4MSC57rlUfZT3oLmWqfWArwvJvct6AsTbnCT8gc0cC3E3X46jnqGiYXXNuf0FluVYlm0Ie_nG6yWqu4jFqURlcHQczBWDNgc2hsjeMfPPQ-MP75QAghPNQ-seNkGv1mzHv9BcbcAjI5K8ehY0TTaw9OWmwa7MP_daYZNvgK-IDy30yUdEn5ye6NNlTZfypxtZNhDsK-TTstIBcQoTIBEq8xKSC4XgLTKNP9b0siiGdZoYIxc5nTQJMjlsPaA" target="_blank" rel="noopener noreferrer"><picture><source media="(prefers-color-scheme: dark)" srcset="https://cursor.com/assets/images/fix-in-web-dark.png"><source media="(prefers-color-scheme: light)" srcset="https://cursor.com/assets/images/fix-in-web-light.png"><img alt="Fix in Web" width="99" height="28" src="https://cursor.com/assets/images/fix-in-web-dark.png"></picture></a></div>


<sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit d2a81eef1a8442c57e5810b600a3dcdf44aa1829. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>


### @gillian-palmer on `src/learning/app/custom_analytics/dashboard_service.py:318` — 2026-05-07T14:37:48Z

```diff
@@ -306,6 +312,24 @@
 WIDGET_TYPES_WITH_DETAILS = frozenset({WidgetType.SINGLE_METRIC})
 
 
+# CORE-587: Multi-channel funnel summary widget gating.
+#
+# The per-channel FUNNEL_SUMMARY widgets (EMAIL/SMS/PUSH) are being replaced by a
+# unified FUNNEL_SUMMARY/BY_CHANNEL widget behind the Statsig gate below. Until the
```

I think this comment isn't necessary, you could just mention all of this in the PR description instead

### @gillian-palmer on `src/learning/app/custom_analytics/dashboard_service.py:442` — 2026-05-07T14:39:38Z

```diff
@@ -358,23 +382,103 @@ def _widget_storage_service(self) -> WidgetStorageService:
     def _statistic_service(self) -> StatisticService:
         return StatisticService()
 
+    def _is_multi_channel_funnel_summary_enabled(self, company_id: str) -> bool:
+        """Check the SCF Statsig gate. Fails closed (returns False) on any error."""
+        if not company_id:
+            return False
+        try:
+            statsig_user = StatsigContext(
+                custom_ids={StatsigUserField.COMPANY_ID: company_id},
+            ).generate_finalized_statsig_user()
+            return bool(
+                statsig_client().check_gate(
+                    statsig_user, _MULTI_CHANNEL_FUNNEL_SUMMARY_GATE
+                )
+            )
+        except Exception as e:
+            with sentry_sdk.push_scope() as scope:
+                scope.set_tag("company_id", company_id)
+                scope.set_tag("gate", _MULTI_CHANNEL_FUNNEL_SUMMARY_GATE)
+                sentry_sdk.capture_exception(e)
+            return False
+
+    def _get_hidden_funnel_summary_subtypes(
+        self, company_id: str
+    ) -> frozenset[WidgetSubtype]:
+        """Return the set of FUNNEL_SUMMARY subtypes hidden for this company."""
+        if self._is_multi_channel_funnel_summary_enabled(company_id):
+            return _FUNNEL_SUMMARY_HIDDEN_WHEN_FLAG_ON
+        return _FUNNEL_SUMMARY_HIDDEN_WHEN_FLAG_OFF
+
+    @staticmethod
+    def _is_funnel_summary_widget_hidden(
+        widget: WidgetData,
+        funnel_summary_hidden: frozenset[WidgetSubtype],
+    ) -> bool:
+        """Funnel-only visibility predicate.
+
+        Note: CORE-586 (SCD) will introduce a deliverability-equivalent. Once both
+        ship, unify into a single `_is_widget_hidden(widget, hidden_by_type)` helper.
+        """
+        return (
+            widget.type == WidgetType.FUNNEL_SUMMARY
+            and widget.subtype in funnel_summary_hidden
+        )
+
     def refresh_dashboard_if_old(
         self,
         company_id: str,
         dashboard: PerformanceDashboardData,
         hours_threshold: int = 8,
     ) -> bool:
         """
-        Returns True if dashbboard is updated, else False
+        Returns True if dashboard should be updated, else False
         """
         if dashboard.last_run:
             update_if_older_than_time = arrow.now().shift(hours=-hours_threshold)
             if dashboard.last_run >= update_if_older_than_time:
-                return False
+                # CORE-587: On a non-stale dashboard, fan out per-widget refreshes
+                # for any *visible* widget marked NOT_RUN (the post-deploy migration
+                # seeds new FUNNEL_SUMMARY/BY_CHANNEL widgets at NOT_RUN). Avoids
```

Nit: should shorten this comment

### @gillian-palmer on `src/learning/app/custom_analytics/dashboard_service.py:462` — 2026-05-07T14:40:39Z

```diff
@@ -358,23 +382,103 @@ def _widget_storage_service(self) -> WidgetStorageService:
     def _statistic_service(self) -> StatisticService:
         return StatisticService()
 
+    def _is_multi_channel_funnel_summary_enabled(self, company_id: str) -> bool:
+        """Check the SCF Statsig gate. Fails closed (returns False) on any error."""
+        if not company_id:
+            return False
+        try:
+            statsig_user = StatsigContext(
+                custom_ids={StatsigUserField.COMPANY_ID: company_id},
+            ).generate_finalized_statsig_user()
+            return bool(
+                statsig_client().check_gate(
+                    statsig_user, _MULTI_CHANNEL_FUNNEL_SUMMARY_GATE
+                )
+            )
+        except Exception as e:
+            with sentry_sdk.push_scope() as scope:
+                scope.set_tag("company_id", company_id)
+                scope.set_tag("gate", _MULTI_CHANNEL_FUNNEL_SUMMARY_GATE)
+                sentry_sdk.capture_exception(e)
+            return False
+
+    def _get_hidden_funnel_summary_subtypes(
+        self, company_id: str
+    ) -> frozenset[WidgetSubtype]:
+        """Return the set of FUNNEL_SUMMARY subtypes hidden for this company."""
+        if self._is_multi_channel_funnel_summary_enabled(company_id):
+            return _FUNNEL_SUMMARY_HIDDEN_WHEN_FLAG_ON
+        return _FUNNEL_SUMMARY_HIDDEN_WHEN_FLAG_OFF
+
+    @staticmethod
+    def _is_funnel_summary_widget_hidden(
+        widget: WidgetData,
+        funnel_summary_hidden: frozenset[WidgetSubtype],
+    ) -> bool:
+        """Funnel-only visibility predicate.
+
+        Note: CORE-586 (SCD) will introduce a deliverability-equivalent. Once both
+        ship, unify into a single `_is_widget_hidden(widget, hidden_by_type)` helper.
+        """
+        return (
+            widget.type == WidgetType.FUNNEL_SUMMARY
+            and widget.subtype in funnel_summary_hidden
+        )
+
     def refresh_dashboard_if_old(
         self,
         company_id: str,
         dashboard: PerformanceDashboardData,
         hours_threshold: int = 8,
     ) -> bool:
         """
-        Returns True if dashbboard is updated, else False
+        Returns True if dashboard should be updated, else False
         """
         if dashboard.last_run:
             update_if_older_than_time = arrow.now().shift(hours=-hours_threshold)
             if dashboard.last_run >= update_if_older_than_time:
-                return False
+                # CORE-587: On a non-stale dashboard, fan out per-widget refreshes
+                # for any *visible* widget marked NOT_RUN (the post-deploy migration
+                # seeds new FUNNEL_SUMMARY/BY_CHANNEL widgets at NOT_RUN). Avoids
+                # the full dashboard refresh path, which would flash already-COMPLETE
+                # widgets back to LOADING on every browser refresh. NOT_RUN is
+                # distinct from LOADING so we don't double-trigger widgets that
+                # already have a task in flight.
+                not_run_widgets = [
+                    w
+                    for w in self._visible_widgets(company_id, dashboard.dashboard_id)
+                    if w.status == WidgetStatus.NOT_RUN
+                ]
+                if not not_run_widgets:
+                    return False
+                triggered = False
+                for widget in not_run_widgets:
+                    try:
+                        self.trigger_widget_update_task(
+                            widget, ignore_concurrency_lock=True
+                        )
+                        triggered = True
+                    except UnableToObtainConcurrencyLockException:
+                        continue
```

This comment is correct, you can get rid of the try except and just run self.trigger_widget_update_task(
                            widget, ignore_concurrency_lock=True
                        )

### @gillian-palmer on `src/learning/app/custom_analytics/dashboard_service.py:439` — 2026-05-07T14:49:35Z

```diff
@@ -370,7 +437,27 @@ def refresh_dashboard_if_old(
         if dashboard.last_run:
             update_if_older_than_time = arrow.now().shift(hours=-hours_threshold)
             if dashboard.last_run >= update_if_older_than_time:
```

Suggestion:
```
def refresh_dashboard_if_old(
      self,
      company_id: str,
      dashboard: PerformanceDashboardData,
      hours_threshold: int = 8,
  ) -> bool:
      """Returns True if dashboard is updated, else False."""
      threshold = arrow.now().shift(hours=-hours_threshold)
      if not dashboard.last_run or dashboard.last_run < threshold:
          self.trigger_dashboard_update_task(company_id, dashboard.dashboard_id)
          return True

      funnel_summary_hidden = self._get_hidden_funnel_summary_subtypes(company_id)
      visible_loading_exists = any(
          widget.status == WidgetStatus.LOADING
          and not self._is_funnel_summary_widget_hidden(widget, funnel_summary_hidden)
          for widget in self._data_service.get_widgets(company_id, dashboard.dashboard_id)
      )
      if not visible_loading_exists:
          return False

      self.trigger_dashboard_update_task(company_id, dashboard.dashboard_id)
      return True
```

### @gillian-palmer on `src/learning/app/custom_analytics/dashboard_service.py:439` — 2026-05-07T14:50:31Z

```diff
@@ -370,7 +437,27 @@ def refresh_dashboard_if_old(
         if dashboard.last_run:
             update_if_older_than_time = arrow.now().shift(hours=-hours_threshold)
             if dashboard.last_run >= update_if_older_than_time:
```

Will make this change in my PR too

### @timothysheee on `src/learning/app/custom_analytics/dashboard_service.py:522` — 2026-05-07T15:44:59Z

```diff
@@ -432,6 +519,41 @@ def get_dashboards(
         """
         return self._data_service.get_dashboards(company_id, dashboard_type)
 
+    def get_total_widget_count(self, company_id: str, dashboard_id: str) -> int:
```

Renamed!

### @timothysheee on `src/learning/app/custom_analytics/dashboard_service.py:572` — 2026-05-07T15:45:00Z

```diff
@@ -447,7 +569,14 @@ def get_widgets(self, company_id: str, dashboard_id: str) -> Iterator[WidgetData
         """
         if not self._data_service.get_dashboard_or_none(company_id, dashboard_id):
             raise PerformanceDashboardDoesNotExist()
-        return self._data_service.get_widgets(company_id, dashboard_id)
+        # CORE-587: hide per-channel funnel widgets when the multi-channel gate is
```

Deleted!

### @timothysheee on `src/learning/app/custom_analytics/dashboard_service.py:451` — 2026-05-07T15:45:03Z

```diff
@@ -370,7 +437,27 @@ def refresh_dashboard_if_old(
         if dashboard.last_run:
             update_if_older_than_time = arrow.now().shift(hours=-hours_threshold)
             if dashboard.last_run >= update_if_older_than_time:
-                return False
+                # CORE-587: Even when last_run is recent, force a refresh if any
+                # *visible* widget is still in LOADING. This handles the case where
+                # the post-deploy migration command seeded a FUNNEL_SUMMARY/BY_CHANNEL
+                # widget (status=LOADING) onto a dashboard whose last_run is recent;
+                # without this, the new widget would render as a perpetual spinner
+                # until the next stale-refresh cycle. Hidden widgets are excluded so
+                # we don't do extra work for widgets the user can't see.
+                funnel_summary_hidden = self._get_hidden_funnel_summary_subtypes(
+                    company_id
+                )
+                visible_loading_exists = any(
+                    widget.status == WidgetStatus.LOADING
```

New WidgetStatus added. https://klaviyo.slack.com/archives/C0A52CWGT7Y/p1778100633175059?thread_ts=1778094385.084149&cid=C0A52CWGT7Y

This was not branched off of Gillian's code. (You can double check the base branch that the PR is comparing to at the top).



### @timothysheee on `src/learning/app/custom_analytics/dashboard_service.py:873` — 2026-05-07T15:45:04Z

```diff
@@ -741,7 +870,32 @@ def create_widget(
         existing_widgets = list(
             self._data_service.get_widgets(company_id, dashboard_id, False)
         )
-        if len(existing_widgets) >= self._MAX_WIDGETS_PER_DASHBOARD:
+        # CORE-587: Enforce the 10-widget limit against the maximum visible count
```

Removed!

### @timothysheee on `src/learning/app/custom_analytics/dashboard_service.py:455` — 2026-05-07T15:45:05Z

```diff
@@ -370,7 +437,27 @@ def refresh_dashboard_if_old(
         if dashboard.last_run:
             update_if_older_than_time = arrow.now().shift(hours=-hours_threshold)
             if dashboard.last_run >= update_if_older_than_time:
-                return False
+                # CORE-587: Even when last_run is recent, force a refresh if any
+                # *visible* widget is still in LOADING. This handles the case where
+                # the post-deploy migration command seeded a FUNNEL_SUMMARY/BY_CHANNEL
+                # widget (status=LOADING) onto a dashboard whose last_run is recent;
+                # without this, the new widget would render as a perpetual spinner
+                # until the next stale-refresh cycle. Hidden widgets are excluded so
+                # we don't do extra work for widgets the user can't see.
+                funnel_summary_hidden = self._get_hidden_funnel_summary_subtypes(
+                    company_id
+                )
+                visible_loading_exists = any(
+                    widget.status == WidgetStatus.LOADING
+                    and not self._is_funnel_summary_widget_hidden(
+                        widget, funnel_summary_hidden
+                    )
+                    for widget in self._data_service.get_widgets(
```

Yes. Done!

### @timothysheee on `src/learning/app/custom_analytics/dashboard_service.py:439` — 2026-05-07T15:45:08Z

```diff
@@ -370,7 +437,27 @@ def refresh_dashboard_if_old(
         if dashboard.last_run:
             update_if_older_than_time = arrow.now().shift(hours=-hours_threshold)
             if dashboard.last_run >= update_if_older_than_time:
```

Done!

### @timothysheee on `src/learning/app/custom_analytics/dashboard_service.py:318` — 2026-05-07T15:45:16Z

```diff
@@ -306,6 +312,24 @@
 WIDGET_TYPES_WITH_DETAILS = frozenset({WidgetType.SINGLE_METRIC})
 
 
+# CORE-587: Multi-channel funnel summary widget gating.
+#
+# The per-channel FUNNEL_SUMMARY widgets (EMAIL/SMS/PUSH) are being replaced by a
+# unified FUNNEL_SUMMARY/BY_CHANNEL widget behind the Statsig gate below. Until the
```

Deleted!

### @timothysheee on `src/learning/app/custom_analytics/dashboard_service.py:442` — 2026-05-07T15:45:17Z

```diff
@@ -358,23 +382,103 @@ def _widget_storage_service(self) -> WidgetStorageService:
     def _statistic_service(self) -> StatisticService:
         return StatisticService()
 
+    def _is_multi_channel_funnel_summary_enabled(self, company_id: str) -> bool:
+        """Check the SCF Statsig gate. Fails closed (returns False) on any error."""
+        if not company_id:
+            return False
+        try:
+            statsig_user = StatsigContext(
+                custom_ids={StatsigUserField.COMPANY_ID: company_id},
+            ).generate_finalized_statsig_user()
+            return bool(
+                statsig_client().check_gate(
+                    statsig_user, _MULTI_CHANNEL_FUNNEL_SUMMARY_GATE
+                )
+            )
+        except Exception as e:
+            with sentry_sdk.push_scope() as scope:
+                scope.set_tag("company_id", company_id)
+                scope.set_tag("gate", _MULTI_CHANNEL_FUNNEL_SUMMARY_GATE)
+                sentry_sdk.capture_exception(e)
+            return False
+
+    def _get_hidden_funnel_summary_subtypes(
+        self, company_id: str
+    ) -> frozenset[WidgetSubtype]:
+        """Return the set of FUNNEL_SUMMARY subtypes hidden for this company."""
+        if self._is_multi_channel_funnel_summary_enabled(company_id):
+            return _FUNNEL_SUMMARY_HIDDEN_WHEN_FLAG_ON
+        return _FUNNEL_SUMMARY_HIDDEN_WHEN_FLAG_OFF
+
+    @staticmethod
+    def _is_funnel_summary_widget_hidden(
+        widget: WidgetData,
+        funnel_summary_hidden: frozenset[WidgetSubtype],
+    ) -> bool:
+        """Funnel-only visibility predicate.
+
+        Note: CORE-586 (SCD) will introduce a deliverability-equivalent. Once both
+        ship, unify into a single `_is_widget_hidden(widget, hidden_by_type)` helper.
+        """
+        return (
+            widget.type == WidgetType.FUNNEL_SUMMARY
+            and widget.subtype in funnel_summary_hidden
+        )
+
     def refresh_dashboard_if_old(
         self,
         company_id: str,
         dashboard: PerformanceDashboardData,
         hours_threshold: int = 8,
     ) -> bool:
         """
-        Returns True if dashbboard is updated, else False
+        Returns True if dashboard should be updated, else False
         """
         if dashboard.last_run:
             update_if_older_than_time = arrow.now().shift(hours=-hours_threshold)
             if dashboard.last_run >= update_if_older_than_time:
-                return False
+                # CORE-587: On a non-stale dashboard, fan out per-widget refreshes
+                # for any *visible* widget marked NOT_RUN (the post-deploy migration
+                # seeds new FUNNEL_SUMMARY/BY_CHANNEL widgets at NOT_RUN). Avoids
```

Deleted!

### @cursor[bot] on `src/learning/app/custom_analytics/dashboard_service.py:424` — 2026-05-07T15:52:00Z

```diff
@@ -367,12 +404,24 @@ def refresh_dashboard_if_old(
         """
         Returns True if dashbboard is updated, else False
         """
-        if dashboard.last_run:
-            update_if_older_than_time = arrow.now().shift(hours=-hours_threshold)
-            if dashboard.last_run >= update_if_older_than_time:
+        threshold = arrow.now().shift(hours=-hours_threshold)
+        if not dashboard.last_run or dashboard.last_run < threshold:
+            try:
+                self.trigger_dashboard_update_task(company_id, dashboard.dashboard_id)
+            except UnableToObtainConcurrencyLockException:
                 return False
-
-        self.trigger_dashboard_update_task(company_id, dashboard.dashboard_id)
+            return True
+
+        not_run_widgets = [
+            w
+            for w in self._visible_widgets(company_id, dashboard.dashboard_id)
+            if w.status == WidgetStatus.NOT_RUN
+        ]
+        if not not_run_widgets:
+            return False
+
+        for widget in not_run_widgets:
+            self.trigger_widget_update_task(widget, ignore_concurrency_lock=True)
```

### Missing exception handling in NOT_RUN widget loop

**Medium Severity**

<!-- DESCRIPTION START -->
The `for` loop calling `self.trigger_widget_update_task(widget, ignore_concurrency_lock=True)` in `refresh_dashboard_if_old` has no `try/except`. If any widget task fails (e.g., from `_spawn_widget_update_task` or `_data_service.update_widget` inside `trigger_widget_update_task`, which catches and re-raises), the exception propagates out, skipping remaining widgets. The test `test_refresh_dashboard_if_old_not_stale_locked_not_run_widget_skipped` explicitly expects graceful per-widget error handling where remaining widgets still get processed, but that behavior is not implemented. The test will fail at runtime.
<!-- DESCRIPTION END -->

<!-- BUGBOT_BUG_ID: cd7f3a4b-6dca-4a5f-9839-58cbd6c23705 -->

<!-- LOCATIONS START
src/learning/app/custom_analytics/dashboard_service.py#L422-L424
tests/custom_analytics/test_dashboard_service.py#L308-L347
LOCATIONS END -->
<details>
<summary>Additional Locations (1)</summary>

- [`tests/custom_analytics/test_dashboard_service.py#L308-L347`](https://github.com/klaviyo/app/blob/9036537b90d732716a499e03c36a3cd5ae42f89f/tests/custom_analytics/test_dashboard_service.py#L308-L347)

</details>

<div><a href="https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..i1aWM7Lq4lLZp-CNywurBbTzidN5aConlhZBoGUYR4g43viqweXXpoQ08tfqJ0H_9uek0gCJpK7N5P0W_v8tXAvihJYYD81Ct3cJttS8fLKKcapjjTKHcoEWHtxeS9VfpURk2mqANh3WxO-WyEMQgYWL6X_kWyP7ZhndTvywe1PMssSE5OlMrq3SfRsa8WiOpt_NEHDX1tUMLgPHMdy5v5ye9eLgNHuldMlxb_ocLcNqqDEFpw-Mpdx6bq3iDwzl6ELAgXaiNRok9QJ7W9DXoAc796DNoOfICf1xj0QbmUSfGvwa54lXCXRYxC0-vdKwzI4PLs9lkONnGTEswyFgYA" target="_blank" rel="noopener noreferrer"><picture><source media="(prefers-color-scheme: dark)" srcset="https://cursor.com/assets/images/fix-in-cursor-dark.png"><source media="(prefers-color-scheme: light)" srcset="https://cursor.com/assets/images/fix-in-cursor-light.png"><img alt="Fix in Cursor" width="115" height="28" src="https://cursor.com/assets/images/fix-in-cursor-dark.png"></picture></a>&nbsp;<a href="https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..WidF7Ab4xOqspc-7rNu7HF6RdVky0XsBGjqy4A-9lShYjGUOID1U-vSY-L0hF4wforFsxBwFK8-xrzhFaeUqWt7dpSFlNkaiFqcyEvWzZepyrLviIN-2QN1ts8rw-ZrZyAeCOqWR8LcQrzHJjNir7fAxz1P4LItqkeXykK6NltisR1yUbAh5bLe8L__VN96I5uGE5b6FA3qkRQxcZW637DhtGsT6YSXkvFjUdqDKXLjPYsgBwx3v0NIoYUV_gvmHD3xicJMcNW9i0cfDMsAnq1NI4Wx1XrxnmFzdNTgRgiB--k0U-7k9rTnlZC5x3aQT-ls_7TabyJLrqUWza42SOg" target="_blank" rel="noopener noreferrer"><picture><source media="(prefers-color-scheme: dark)" srcset="https://cursor.com/assets/images/fix-in-web-dark.png"><source media="(prefers-color-scheme: light)" srcset="https://cursor.com/assets/images/fix-in-web-light.png"><img alt="Fix in Web" width="99" height="28" src="https://cursor.com/assets/images/fix-in-web-dark.png"></picture></a></div>


<sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 9036537b90d732716a499e03c36a3cd5ae42f89f. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>


### @cursor[bot] on `tests/custom_analytics/test_dashboard_service.py:1363` — 2026-05-07T15:52:00Z

```diff
@@ -1087,6 +1251,428 @@ def test_get_widget_signed_s3_url(self, dashboard_data, widget_data, service):
 
         assert mock_url_value == result_url
 
+    # ----------------------------------------------------------------------
+    # CORE-587: multi-channel funnel summary widget gating
+    # ----------------------------------------------------------------------
+    @pytest.mark.parametrize(
+        "flag_on,visible_subtypes,hidden_subtypes",
+        [
+            (
+                True,
+                [WidgetSubtype.BY_CHANNEL],
+                [WidgetSubtype.EMAIL, WidgetSubtype.SMS, WidgetSubtype.PUSH],
+            ),
+            (
+                False,
+                [WidgetSubtype.EMAIL, WidgetSubtype.SMS, WidgetSubtype.PUSH],
+                [WidgetSubtype.BY_CHANNEL],
+            ),
+        ],
+        ids=["flag_on_hides_per_channel", "flag_off_hides_by_channel"],
+    )
+    def test_get_widgets_filters_funnel_summary_by_flag(
+        self,
+        service,
+        dashboard_data,
+        flag_on,
+        visible_subtypes,
+        hidden_subtypes,
+    ):
+        """get_widgets hides funnel widgets according to the SCF Statsig gate."""
+        all_subtypes = [
+            WidgetSubtype.EMAIL,
+            WidgetSubtype.SMS,
+            WidgetSubtype.PUSH,
+            WidgetSubtype.BY_CHANNEL,
+        ]
+        widgets = [
+            WidgetFactory.build(
+                type=WidgetType.FUNNEL_SUMMARY, subtype=subtype, order=i
+            )
+            for i, subtype in enumerate(all_subtypes)
+        ]
+        service._data_service.get_dashboard_or_none.return_value = dashboard_data
+        service._data_service.get_widgets.return_value = widgets
+
+        with patch.object(
+            PerformanceDashboardService,
+            "_is_multi_channel_funnel_summary_enabled",
+            return_value=flag_on,
+        ):
+            result = list(service.get_widgets(COMPANY_ID, DASHBOARD_ID))
+
+        result_subtypes = {w.subtype for w in result}
+        assert result_subtypes == set(visible_subtypes)
+        assert not (result_subtypes & set(hidden_subtypes))
+
+    def test_get_widgets_does_not_filter_non_funnel_widgets(
+        self, service, dashboard_data
+    ):
+        """Non-funnel widgets (e.g. DELIVERABILITY) are unaffected by the SCF gate."""
+        non_funnel = [
+            WidgetFactory.build(
+                type=WidgetType.DELIVERABILITY, subtype=WidgetSubtype.EMAIL, order=0
+            ),
+            WidgetFactory.build(
+                type=WidgetType.PERFORMANCE, subtype=WidgetSubtype.CAMPAIGN, order=1
+            ),
+        ]
+        service._data_service.get_dashboard_or_none.return_value = dashboard_data
+        service._data_service.get_widgets.return_value = non_funnel
+
+        with patch.object(
+            PerformanceDashboardService,
+            "_is_multi_channel_funnel_summary_enabled",
+            return_value=True,
+        ):
+            result = list(service.get_widgets(COMPANY_ID, DASHBOARD_ID))
+
+        assert len(result) == 2
+
+    def test_funnel_summary_gate_failure_fails_closed(self, service):
+        """If the Statsig client raises, the gate evaluates to False (closed)."""
+        with patch(
+            "app.custom_analytics.dashboard_service.statsig_client",
+            side_effect=Exception("statsig down"),
+        ):
+            assert service._is_multi_channel_funnel_summary_enabled(COMPANY_ID) is False
+
+    @pytest.mark.parametrize(
+        "flag_on,expected_hidden",
+        [
+            (
+                True,
+                frozenset({WidgetSubtype.EMAIL, WidgetSubtype.SMS, WidgetSubtype.PUSH}),
+            ),
+            (False, frozenset({WidgetSubtype.BY_CHANNEL})),
+        ],
+        ids=["flag_on", "flag_off"],
+    )
+    def test_get_hidden_funnel_summary_subtypes(
+        self, service, flag_on, expected_hidden
+    ):
+        """Helper returns the right hidden-subtype set for each gate state."""
+        with patch.object(
+            PerformanceDashboardService,
+            "_is_multi_channel_funnel_summary_enabled",
+            return_value=flag_on,
+        ):
+            assert (
+                service._get_hidden_funnel_summary_subtypes(COMPANY_ID)
+                == expected_hidden
+            )
```

### Test calls non-existent method `_get_hidden_funnel_summary_subtypes`

**Medium Severity**

<!-- DESCRIPTION START -->
The test `test_get_hidden_funnel_summary_subtypes` calls `service._get_hidden_funnel_summary_subtypes(COMPANY_ID)`, but this method does not exist on `PerformanceDashboardService`. A search of the entire codebase confirms no definition exists. This test will fail with an `AttributeError` at runtime. The method was likely planned but either renamed or dropped during development.
<!-- DESCRIPTION END -->

<!-- BUGBOT_BUG_ID: e3c3a5fc-17c1-438b-84af-a94bfd9cd4c2 -->

<!-- LOCATIONS START
tests/custom_analytics/test_dashboard_service.py#L1350-L1363
LOCATIONS END -->
<div><a href="https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..WZLasIAB0fPCYYKb89A7AKWYRRSSPj9QQPt4zxUEVXkFZkXn1xJ9eCKxNvpys1cuqyq3BBGrdrwTKdarB73ZwI8d6zZtg2REW-fZH4_txn9-2HFlVg6k6sTc-tQLq_5butrMNb6QUV7kZstxJayZPe1r_lZN9j7FpQcCaaLbBiV7JvqQ0gFmlNzXnTDhibL7J2gZiw6Z6BAcDU44gQEGJqXa6wLEKxfSzOgaETLaNdoHZQUZ9KpNpoY6j4RrRBhuHg12-i42VVV-i-MhQKaq10bOEhYj8EUyK06kyxFgIUVrd6MNYl5XMPyMkm5X33qeIBaFPWQyBWts3zFsYTgQMA" target="_blank" rel="noopener noreferrer"><picture><source media="(prefers-color-scheme: dark)" srcset="https://cursor.com/assets/images/fix-in-cursor-dark.png"><source media="(prefers-color-scheme: light)" srcset="https://cursor.com/assets/images/fix-in-cursor-light.png"><img alt="Fix in Cursor" width="115" height="28" src="https://cursor.com/assets/images/fix-in-cursor-dark.png"></picture></a>&nbsp;<a href="https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..umWhVOTSCNuwsKhRvgJgc0BtI9FwV4y6S8OIt1TZGaVh7LPFRoMAd4Npjas_2ri41zQKIUZRjq4odo-GdTvZWdqpIRgD5S0HPVqWV8OIz35Vurk1SOiW5hcND6DcqmnhYZwP1yNDDQ4kYzfi_VIdiyWID5xNh9NbF62-w-rqnzxhn7lb7Q6KmbgKwE8P4CBKyNCie1pQLU6v7euy7Zp7xnGS5VjGFlK2byNuqrKD7RliakO_3gqoX8bkj9iPVfMJDEFdWaSCQe8vXCZCLaIOiySuRp_mgd4lpZ3370My6kAMQ1wuEDBHrHj7cUPUw5BUmNNaUswXKs1bkXOpCgdnKQ" target="_blank" rel="noopener noreferrer"><picture><source media="(prefers-color-scheme: dark)" srcset="https://cursor.com/assets/images/fix-in-web-dark.png"><source media="(prefers-color-scheme: light)" srcset="https://cursor.com/assets/images/fix-in-web-light.png"><img alt="Fix in Web" width="99" height="28" src="https://cursor.com/assets/images/fix-in-web-dark.png"></picture></a></div>


<sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 9036537b90d732716a499e03c36a3cd5ae42f89f. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>


### @timothysheee on `src/learning/app/custom_analytics/dashboard_service.py:1134` — 2026-05-07T16:34:16Z

```diff
@@ -1061,7 +1168,8 @@ def trigger_dashboard_update_task(
             self._concurrency_lock.release(company_id, dashboard_id)
             raise PerformanceDashboardDoesNotExist()
 
-        widgets = self._data_service.get_widgets(company_id, dashboard_id)
+        # Skip hidden widgets, so we don't enqueue celery work for hidden widgets
+        widgets = list(self._visible_widgets(company_id, dashboard_id))
```

Converted to list, so the following try/catch work.
- try - iterates once
- catch - re-iterates (and might be in the middle of iterating widgets)

### @timothysheee on `src/learning/app/custom_analytics/dashboard_service.py:510` — 2026-05-07T16:36:43Z

```diff
@@ -432,6 +493,29 @@ def get_dashboards(
         """
         return self._data_service.get_dashboards(company_id, dashboard_type)
 
+    def get_visible_widget_count(self, company_id: str, dashboard_id: str) -> int:
+        """Get number of visible widgets for a dashboard based on which widgets are hidden behind feature flags"""
+        if not self._data_service.get_dashboard_or_none(company_id, dashboard_id):
+            raise PerformanceDashboardDoesNotExist()
+
+        existing_widgets = list(
+            self._data_service.get_widgets(company_id, dashboard_id)
+        )
+        return max(
+            sum(
+                1
+                for _ in self._filter_visible(
+                    existing_widgets, _FUNNEL_SUMMARY_HIDDEN_WHEN_FLAG_ON
+                )
+            ),
```

question: should this be moved to a utility? Current thought is not do so since it's only used twice

### @cursor[bot] on `src/learning/app/custom_analytics/dashboard_service.py:817` — 2026-05-07T16:59:54Z

```diff
@@ -741,7 +810,11 @@ def create_widget(
         existing_widgets = list(
             self._data_service.get_widgets(company_id, dashboard_id, False)
         )
-        if len(existing_widgets) >= self._MAX_WIDGETS_PER_DASHBOARD:
+        # +1 for the widget being added; it's not yet in `existing_widgets`.
+        if (
+            self._get_max_visible_count(existing_widgets) + 1
+            > self._MAX_WIDGETS_PER_DASHBOARD
+        ):
```

### Widget limit check over-counts when new widget is flag-hidden

**Medium Severity**

<!-- DESCRIPTION START -->
The `create_widget` limit check computes `_get_max_visible_count(existing_widgets) + 1`, unconditionally adding `+1` for the new widget. But the new widget might be hidden in the flag state that produces the maximum. For example, with 9 non-funnel widgets + 1 EMAIL funnel, `_get_max_visible_count` returns `max(9, 10) = 10`. Adding a BY_CHANNEL funnel gives `10 + 1 = 11 > 10`, which falsely rejects. The correct post-add max is `max(flag-on: 9+1=10, flag-off: 10+0=10) = 10` — at the limit, not over it. The new widget's type/subtype needs to be included in the per-flag-state count rather than added as a blanket `+1`. The test `test_create_widget_allows_funnel_swap_at_limit` also confirms the intended behavior but would fail with this logic.
<!-- DESCRIPTION END -->

<!-- BUGBOT_BUG_ID: 9a4cdc16-ea8c-41e5-88d5-a6abbb5852de -->

<!-- LOCATIONS START
src/learning/app/custom_analytics/dashboard_service.py#L813-L817
src/learning/app/custom_analytics/dashboard_service.py#L1232-L1249
LOCATIONS END -->
<details>
<summary>Additional Locations (1)</summary>

- [`src/learning/app/custom_analytics/dashboard_service.py#L1232-L1249`](https://github.com/klaviyo/app/blob/048aa5cc136891445aac6430b9a5d6411caca63a/src/learning/app/custom_analytics/dashboard_service.py#L1232-L1249)

</details>

<div><a href="https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..O_VGHZQJ91YuCkeClk40LRWpvRAvcCh_hPyozt0-uQZhGF_ArtzJSlB2CaFlF0AUsahtHbjGj21-tlrfBRFbiYWRnGIeRPgAw4XWVxy1i5BFHTbVvOgrZN-yo56J92tHsMWjSTOwIqPNVKFiXgUhUFuSBHH1Ksz7UeX_cQlE1fRwbcc-N9bnH7zy1KTQ7c8gdK7Ohx12A41TZXz_VMB6Nyc_Ct3JlW0QJc_E55IpYczaeCyNrx07AH4v_a6h3ayrzDJHJg1zZMuAzu_fUjI9ZmFto29RBxDDYOpLB3PVFbjiRom9rtEvA0DzYrjxH8Qt8Gjvh8rhlLeSb6-gvgJAWA" target="_blank" rel="noopener noreferrer"><picture><source media="(prefers-color-scheme: dark)" srcset="https://cursor.com/assets/images/fix-in-cursor-dark.png"><source media="(prefers-color-scheme: light)" srcset="https://cursor.com/assets/images/fix-in-cursor-light.png"><img alt="Fix in Cursor" width="115" height="28" src="https://cursor.com/assets/images/fix-in-cursor-dark.png"></picture></a>&nbsp;<a href="https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..bueHGQVLEbOaGIaETReUyOnCKG7lj48Y6Lxt_FKFlZaLcn62Wp3P6BV5PN8FgSq924exOSfdeV9BV6G5oc2FYFPhGkfuQpAARjG_RinW7ZWSjiHzAzGvwhaIwbZOTa5N7lUXaXxbJpFn9z8KKB5PniaEk4_cXaXYr5lC4LiNKaVGcS9NMZrtvyNPhvWAGXVLHVszvzUgEeaLY7xX_DDRyTCXWEyuOQ9pQ3FGmysrpmKlHsiD7hBl2lRED4Rf3Cf-UEQBpfm4eMtOtvcR42G7-w3naC2tUvd_BfGK_8g-ZfeTJ1lkqngiJo3HSxAFVJ4y4R7OXY0Gc_yLF7E4hMYgkw" target="_blank" rel="noopener noreferrer"><picture><source media="(prefers-color-scheme: dark)" srcset="https://cursor.com/assets/images/fix-in-web-dark.png"><source media="(prefers-color-scheme: light)" srcset="https://cursor.com/assets/images/fix-in-web-light.png"><img alt="Fix in Web" width="99" height="28" src="https://cursor.com/assets/images/fix-in-web-dark.png"></picture></a></div>


<sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 048aa5cc136891445aac6430b9a5d6411caca63a. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>


---

## Issue-Level Comments (PR conversation)

### @linear-code[bot] — 2026-05-04T15:46:33Z

<!-- linear-linkback -->
<details>
<summary><a href="https://linear.app/klaviyo/issue/CORE-587/scf-migrate-to-multi-channel-funnel-summary-widget">CORE-587 [SCF] Migrate to multi-channel funnel summary widget</a></summary>
<p>

### User story

As a Klaviyo customer, I want my Performance Dashboard to show a single unified funnel summary widget covering all send channels instead of separate per-channel widgets, so that I can see all my funnel data in one place.

### Background

Today, dashboards contain separate Email, SMS, and Push funnel summary widgets. This ticket replaces them with a single BY_CHANNEL widget using the same phased approach as the SCD migration — code ships first, a database migration runs post-deploy, and then the feature flag is gradually ramped to expose the new widget to users. The old per-channel rows are not deleted here; they coexist invisibly in the background until a follow-on cleanup ticket.

This ticket follows the same strategy as the SCD migration ([CORE-586](https://linear.app/klaviyo/issue/CORE-586/scd-migrate-to-multi-channel-deliverability-widget)). SCF-specific flag names and subtype values should be confirmed before implementation begins.

### What needs to happen

**Before the migration runs**, the backend needs to understand the SCF feature flag and serve the right widget based on it — the new BY_CHANNEL widget when the flag is on, and the old per-channel widgets when it's off. Supporting changes include: the widget limit check accounting for the temporary coexistence of multiple rows per dashboard, new dashboards getting the correct widget type, and newly created BY_CHANNEL widgets processing automatically on first visit rather than waiting for the next scheduled refresh.

Note: if the loading state staleness fix was already introduced as part of the SCD migration, confirm it also covers funnel summary widgets — it may need only a minor extension rather than a full re-implementation.

**After the code is deployed**, a database migration inserts a BY_CHANNEL funnel widget for every dashboard that currently has a per-channel funnel widget. Runs in batches and is safe to re-run.

**Once the migration is complete**, the feature flag is ramped gradually with monitoring between steps.

### Rollback

Disable the flag at any point. The old per-channel rows remain in the database throughout this ticket.

### What to watch during ramp

* First-load spinners on the new widget are expected on first visit per company — resolves automatically
* Any dashboard showing zero funnel summary widgets (filtering bug)
* Errors rendering the new widget after the spinner resolves
* Celery queue depth for the new funnel summary processor

---

### Open questions & considerations

**Widget ordering**

We need the BY_CHANNEL widget to appear in the same position the user's existing funnel widgets occupied — not appended to the end. The migration sets BY_CHANNEL's position to match the first per-channel funnel widget at the time the migration runs (using the minimum position across Email, SMS, Push). However, there is a window between when the migration runs and when the flag is enabled during which a user could reorder their funnel widgets — BY_CHANNEL won't follow since it's hidden. Questions to resolve:

* Do we need a pre-ramp order sync immediately before enabling the flag, to re-align BY_CHANNEL's position?
* After the legacy rows are deleted (in the follow-on ticket), the order sequence will have gaps where the old per-channel rows used to be. Are orders renormalized on dashboard load? Does a gap cause rendering or reorder issues?
* Context: same as SCD — the existing `delete_widget` behavior doesn't update sibling positions, gaps are endemic in production (max observed position is 29 with a 10-widget limit), and the frontend handles gaps gracefully. But worth confirming nothing specific to SCF changes this picture, especially since SCF can remove up to 3 rows (Email + SMS + Push), leaving a larger gap than SCD.

**Migration scale and operational safety**

The widget table has approximately 37M rows; the performance dashboard table has approximately 6M rows. Things to confirm:

* Test against a database snapshot before running in production — confirm runtime estimate on a clone.
* The INSERT migration must use keyset pagination to avoid expensive full-table scans on each batch iteration. Confirm the correct index is available.
* PTOSC does not apply here — per the [eng-handbook](<https://github.com/klaviyo/eng-handbook/blob/master/databases/migrations/ptosc.md>), PTOSC is for DDL schema changes, not DML data migrations like this. The same operational discipline still applies: batched execution outside a transaction, DB snapshot first, monitor DML latency.
* Confirm with the platform team whether the migration runs inline at deploy or as a post-deploy background step.
* SCF may affect more rows per dashboard than SCD (up to 3 per-channel rows vs. 2) — factor this into the runtime estimate.

### Dependencies

See linked work items
</p>
</details>
<!-- linear-review-link -->
<p><a href="https://linear.app/klaviyo/review/featapex-core-587-multi-channel-funnel-summary-widget-pre-migration-5021d83dc92f">Review in Linear</a></p>


### @klaviyo-code-delivery[bot] — 2026-05-04T18:41:06Z

## MyPy Check Results

### ⚠️ **Non-Blocking - Non-Registry Files with Errors:**
  - `src/learning/app/custom_analytics/dashboard_service.py`

---
ℹ️ These errors are **informative only** and won't fail your build.

To see these errors locally, run:
```bash
git diff --name-only HEAD master | grep '\.py$' | grep -v '^tests/' | xargs mypy
```

### @klaviyo-code-delivery[bot] — 2026-05-04T19:04:04Z

## Test Coverage and Code Quality

Diff coverage for this PR is 100%.

https://klaviyo-ci-cache.s3.amazonaws.com/app/PR-116708/323274/diff-cover

### McCabe Cyclomatic Complexity
> Top 10 most complex functions in files changed in this PR. Only scores above 5 are shown.
> A complexity score of 10 or more is considered high and you should consider refactoring the code. See docs for more information on McCabe Complexity


| Score | File | Location |
|-------|------|----------|
| 12 | src/learning/app/custom_analytics/views/performance_dashboards.py | 386:4: WidgetViewSet.partial_update |
| 11 | src/learning/app/custom_analytics/dashboard_service.py | 634:4: PerformanceDashboardService.update_qgr_dashboard |
| 9 | src/learning/app/custom_analytics/views/performance_dashboards.py | 159:4: PerformanceDashboardViewSet.create |
| 8 | src/learning/app/custom_analytics/dashboard_service.py | 1143:4: PerformanceDashboardService.trigger_dashboard_update_task |
| 8 | src/learning/app/custom_analytics/dashboard_service.py | 779:4: PerformanceDashboardService.create_widget |
| 8 | src/learning/app/custom_analytics/views/performance_dashboards.py | 104:4: PerformanceDashboardViewSet.partial_update |
| 7 | src/learning/app/custom_analytics/dashboard_service.py | 1318:4: PerformanceDashboardService.process_widget |
| 7 | src/learning/app/custom_analytics/dashboard_service.py | 398:4: PerformanceDashboardService.refresh_dashboard_if_old |
| 7 | src/learning/app/custom_analytics/views/performance_dashboards.py | 289:4: PerformanceDashboardConfigViewSet.partial_update |


### @klaviyo-code-delivery[bot] — 2026-05-07T14:05:22Z

We recommend using our ephemeral environments to test this PR in a dedicated preview environment. You can spin one up by adding the *ephemeral-env* label to your PR and pushing a commit (can be empty).
[See docs here.](https://github.com/klaviyo/k-repo/blob/main/python/klaviyo/step_service/assets/ephemeral_environments.md)

### @timothysheee — 2026-05-07T17:30:27Z

@danieladukeshire 
Thank you for your patience with the AI Code reviewers. I rewrote a decent chunk by hand and would love for a re-review!

<details open>
<summary><h2>Changes</h2></summary>

- delete extraneous comments
- cache feature gate
- flatten `refresh_dashboard_if_old`
- create visibility abstraction to replace code
  - `_filter_visible`
  - `_visible_widgets`
- delete tests that are no longer applicable
- create new tests
  - cache
  - validate `refresh_dashboard_if_old`

</details>

Same as before, feel free to leave any and all comments!


