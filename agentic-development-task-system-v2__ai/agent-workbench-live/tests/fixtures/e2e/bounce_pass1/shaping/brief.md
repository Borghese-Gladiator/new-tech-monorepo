# Brief — Goodbye command

## Goal

Add a `goodbye` subcommand to `bin/cli`.

## Acceptance criteria

- AC-1: `bin/cli goodbye` exits 0 and prints `goodbye, world` on stdout.

## Files likely to change

- `bin/cli`
